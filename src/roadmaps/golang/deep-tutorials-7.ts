import type { DeepTutorial } from '../deep-types';

export const GOLANG_DEEP_TUTORIALS_7: Record<string, DeepTutorial> = {
  "4PrkkoZ5fY-oow0O-bVhu": { mentalModel: 'Go 是 Google 为"云原生时代"设计的语言——简单（25 个关键字）、快速编译、天生支持并发。不像 C++ 有 30 年包袱，不像 Java 要 JVM，不像 Python 慢。云基础设施（Docker/K8s/Terraform）都是 Go 写的，不是巧合。', sections: [{ title: 'Go 的设计哲学', content: '1) Simplicity——新手 1 周写项目 2) Fast compile——几秒编译完，开发体验像脚本语言 3) 内置并发——goroutine + channel 是语言特性（不是库）4) 单一二进制——编译成一个文件拷到服务器直接跑 5) 强类型但不啰嗦——:= 自动推导类型。Go 牺牲了"表达力"换"可维护性"——10 个人的团队写的 Go 代码都差不多。' }], diagnosis: [{ symptom: 'go build 报错"package ... is not in GOROOT"', cause: 'import 路径写错或未安装对应依赖，编译器在 GOROOT 和 GOPATH 中都找不到该包。', fix: 'go mod tidy 检查依赖。第三方包需 go get 下载。检查 import 路径拼写是否正确。' }, { symptom: '编译成功但二进制文件在目标机器上无法运行', cause: '交叉编译时 GOOS/GOARCH 设置错误，或 CGO_ENABLED=1 导致动态链接了本地库。', fix: 'CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o app . 确保纯静态编译。用 file 命令验证二进制类型。' }], exercises: [{ level: '基础', task: 'Go 相比 Java 部署方面最大的优势是什么', hint: '编译产物', answer: 'Go 编译成单一静态链接二进制文件→复制到服务器→运行。不需要 JDK/JVM/Classpath/依赖管理。一个文件就是完整的可部署单元。' }] },
  "MBafBhpEV_7qgDNPOIBBs": { mentalModel: 'Go 函数基础——func 关键字+函数名+参数列表+返回值类型。Go 函数是一等公民（可以当参数传、当返回值），函数名大小写决定可见性（大写=exported，小写=包内私有）。', sections: [{ title: '函数定义', content: '基本：func add(a, b int) int { return a + b }。多返回值：func split(sum int) (x, y int) { x = sum/2; y = sum - x; return }（named return——return 语句自动返回命名返回值）。Variadic：func sum(nums ...int) int——接收任意数量参数。函数作为参数：func apply(fn func(int, int) int, a, b int) int { return fn(a, b) }。' }], diagnosis: [{ symptom: '函数返回多值但只接收了一个值', cause: 'Go 支持多返回值但调用者必须接收所有值，否则编译报错。', fix: '用 _ 忽略不需要的返回值：result, _ := fn()。或全部接收：a, b, c := fn()。' }, { symptom: '函数作为参数时类型不匹配编译失败', cause: 'Go 函数签名必须完全匹配，包括参数类型、数量和返回值类型。', fix: '检查函数签名：func(fn func(int, int) int)。参数数量和返回类型必须与传入的函数一致。' }], exercises: [{ level: '基础', task: '写一个函数接收 int 切片返回最大值和最小值', hint: '多返回值', answer: 'func minMax(nums []int) (int, int) { min, max := nums[0], nums[0]; for _, n := range nums { if n < min { min = n }; if n > max { max = n } }; return min, max }' }] },
  "HIvwT8Dcr6B2NjOjy9hFv": { mentalModel: 'Named Return Values——给返回值起名字，像"在函数签名里声明变量"。return 语句可以不写变量名（自动返回当前值）。让函数文档更好但滥用会降低可读性。', sections: [{ title: 'Named Return 用法', content: 'func divide(a, b int) (quotient, remainder int) { quotient = a / b; remainder = a % b; return }。裸 return（不带变量名）适合短函数。长函数建议显式 return quotient, remainder——不然读者要翻到函数签名才知道返回什么。' }], diagnosis: [{ symptom: '裸 return 返回了非预期的零值', cause: '命名返回值在函数开始时初始化为零值，如果代码路径未给命名返回值赋值就执行了裸 return，返回的是零值而非预期值。', fix: '长函数（>10 行）显式 return 变量名。关键路径避免裸 return。在函数开头检查所有命名返回值是否都有赋值。' }, { symptom: 'defer 修改命名返回值但外部没看到变化', cause: 'defer 在 return 之后执行，但命名返回值允许 defer 修改最终返回值——只有在使用命名返回值时 defer 才能修改返回值。', fix: '利用此特性：func fn() (result int) { defer func() { result++ }(); return 0 } // 返回 1。确认这是期望行为。' }], exercises: [{ level: '基础', task: '什么时候应该避免裸 return', hint: '函数太长', answer: '函数超过 5-10 行时应该显式 return 变量名。裸 return 在长函数中让维护者不知道到底返回了什么值——必须翻到函数签名才能看到命名返回值。' }] },
  "MUJfa0jIL_S_b2ndNpVVw": { mentalModel: '变参函数是"参数个数不确定"的函数——... 操作符像大口吃豆子，接收 0 到 N 个参数。fmt.Println 就是变参的典型。"我可以吃任意多个参数！"', sections: [{ title: '变参函数', content: '定义：func sum(nums ...int) int。nums 在函数内部是 []int 类型。调用：sum(1, 2, 3) 或 sum(slice...)。slice... 把切片展开成参数列表。和 Python 的 *args 类似。只能有最后一个参数是变参。' }], diagnosis: [{ symptom: '将切片传给变参函数时忘记展开符 ...', cause: 'func fn(args ...int) 接收的是 ...int 而非 []int，直接传切片类型不匹配。', fix: 'fn(slice...) 用展开符将切片展开为独立参数。fn(slice) 会编译报错：cannot use slice (type []int) as type int。' }, { symptom: '变参函数内部修改切片元素影响调用方', cause: 'nums ...int 在函数内部是 []int 切片，如果调用方传入切片展开，底层数组可能共享。', fix: '如果不想影响原切片，在函数内部 copy 一份：n := make([]int, len(nums)); copy(n, nums)。' }], exercises: [{ level: '基础', task: '写一个变参函数 concatenate(strings ...string) string 把所有字符串拼接起来', hint: 'strings.Join', answer: 'func concatenate(strs ...string) string { return strings.Join(strs, "") }。调用：concatenate("a", "b", "c") → "abc"。注意空参数调用返回空字符串。' }] },
  "2XaDvTJ5pyChgt1GFZF8W": { mentalModel: 'if-else 是 Go 的"路口判断"——条件表达式不加括号（Go 风格），大括号必须（不允许一行 if）。if 语句可以带一个初始化语句：if err := do(); err != nil { ... }。', sections: [{ title: 'if-else 关键点', content: 'if 初始化语句——变量作用域仅在 if-else 块内：if x := compute(); x > 0 { ... } else { ... }。Go 没有三元运算符（?:）——故意的设计选择（可读性优先）。if err != nil——这是 Go 最常见的模式。' }], diagnosis: [{ symptom: 'if 初始化语句中的变量在 if/else 块外不可用', cause: 'if 初始化语句声明的变量作用域仅限于 if-else 块及其所有分支。', fix: '如需外部使用：在 if 前声明，if 内用 = 而非 :=。var data []byte; var err error; if data, err = load(); err != nil { ... }。' }, { symptom: 'if 条件中赋值而非比较（Go 编译器会阻止）', cause: 'if x = 5 { ... } 中 = 是赋值，但 Go 要求 if 条件必须是 bool 类型，因此大多数情况编译器会报错。唯一例外是 x 是 bool 类型时。', fix: '使用 == 而非 =。go vet 会检测可疑的 if 赋值。如果 x 恰为 bool，if x = fn() 可能编译通过但逻辑错误。' }], exercises: [{ level: '基础', task: 'Go 为什么没有三元运算符', hint: '可读性', answer: 'Go 设计者认为三元运算符让代码难以阅读（嵌套三元尤其糟糕）。Go 的选择是用 if-else 明确表达——多写几行但一眼就懂。' }] },
  "C7wzT161ytHsCpO2zfmXo": { mentalModel: 'Go 的 switch 比 C 系语言更强——不需要写 break（自动 fallthrough 不执行）、可以匹配多个值、可以无表达式（替代 if-else-if 链）、类型 switch 匹配变量类型。', sections: [{ title: 'Switch 高级用法', content: '多值匹配：case "Mon", "Tue", "Wed":。无表达式：switch { case x > 0: ... case x < 0: ... }（替代长 if-else 链）。Type switch：switch v := x.(type) { case int: ... case string: ... }。fallthrough：强制执行下一个 case（很少用）。' }], diagnosis: [{ symptom: 'switch case 穿透（fallthrough）到错误分支', cause: 'Go 默认不 fallthrough，但使用 fallthrough 关键字时会强制执行下一个 case 的全部代码（不检查下一个 case 的条件）。', fix: '避免使用 fallthrough，用逗号分隔的多值匹配：case 1, 2, 3:。fallthrough 只能穿透到下一个 case，不能跨多个。' }, { symptom: 'type switch 中 case 多个类型导致变量 v 类型不确定', cause: 'case int, float64: 时 v 的类型是 interface{} 而非匹配的具体类型，无法直接做算术运算。', fix: '为每种类型单独写 case。或对通用类型重新断言：case int, float64: switch n := v.(type) { case int: ... case float64: ... }。' }], exercises: [{ level: '基础', task: '用无表达式 switch 替代 if-else-if 链', hint: 'switch { case cond1: ... case cond2: ... }', answer: 'switch { case score >= 90: grade = "A"; case score >= 80: grade = "B"; case score >= 70: grade = "C"; default: grade = "F" }' }] },
  "IAXI7OAAAG4fU6JvVNSZI": { mentalModel: 'Rune 是 Go 对 Unicode 的处理——rune 就是 int32 的别名，代表一个 Unicode Code Point。处理中文/emoji 用 rune 不用 byte——一个中文字是 3 个 byte 但 1 个 rune。', sections: [{ title: 'Rune vs Byte', content: 's := "你好" → len(s) = 6（UTF-8 编码：每个中文 3 字节）。len([]rune(s)) = 2（字符数）。遍历字符串：for i, r := range s 按 rune 遍历；for i := 0; i < len(s); i++ 按 byte 遍历。rune 字面量用 \'\' 单引号：r := \'你\'。' }], diagnosis: [{ symptom: '用 s[i] 索引中文字符串返回部分字节乱码', cause: 's[i] 返回的是第 i 个 byte，中文字符占 3 字节，索引中间位置返回的是字符的中间字节。', fix: '按 rune 遍历：for _, r := range s。随机访问第 n 个字符：[]rune(s)[n]。注意：[]rune(s) 是 O(n) 操作（需遍历全部）。' }, { symptom: '截取字符串前 n 个字符时截断了多字节字符', cause: 's[:3] 按字节截取，如果第 3 字节在多字节字符中间，会截断该字符产生非法 UTF-8 字符串。', fix: '用 for range 循环计数字符边界：for i := range s { if count == n { return s[:i] }; count++ }。或转 []rune 后截取再转 string。' }], exercises: [{ level: '基础', task: '统计一段中文字符串的实际字符数', hint: 'len([]rune(s))', answer: 's := "你好世界"; fmt.Println(len([]rune(s))) // 4。len(s) 返回的是字节数 12。' }] },
  "PC-azlCZ6wDCn1_nRyhUn": { mentalModel: '遍历字符串要小心——for range 遍历 rune，for i++ 遍历 byte。中文/emoji 用 range 才能正确处理。', sections: [{ title: '字符串遍历', content: '正确（rune 遍历）：for i, r := range "你好" → i=0 r=\'你\'、i=3 r=\'好\'（i 是字节偏移不是字符索引）。错误（byte 遍历）：for i := 0; i < len(s); i++ → 每次取一个 byte，中文会乱码。' }], diagnosis: [{ symptom: 'for range 遍历时索引不连续（0, 3 而非 0, 1）', cause: 'range string 按 rune 遍历，索引 i 是 rune 起始位置的字节偏移量。中文字每个 3 字节，所以索引从 0 跳到 3。', fix: '如果需要字符序号，自己维护计数变量：count := 0; for _, r := range s { count++; fmt.Println(count, r) }。' }, { symptom: '修改 range 中的 v（rune 副本）不影响原字符串', cause: 'for _, r := range s 中 r 是 rune 的副本，修改 r 不影响原字符串。而且 Go 字符串不可变。', fix: '需要修改字符串：先转 []rune，修改后转回 string：rs := []rune(s); rs[i] = newRune; s = string(rs)。' }], exercises: [{ level: '基础', task: '为什么 range 遍历字符串时 index 不是 0, 1, 2, 3 而是 0, 3?', hint: 'UTF-8 编码', answer: '中文字"你"的 UTF-8 编码是 3 个字节（E4 BD A0）。range 按 rune 遍历→第一个 rune 从 byte 0 开始→占 3 字节→下一个 rune 从 byte 3 开始。index 是字节偏移量不是字符数。' }] },
  "7h08RUNC_uAMI6vnGdcZ1": { mentalModel: '解释型字符串字面量（Interpreted String Literals）是用双引号 "" 包起来的字符串——支持转义字符（\\n \\t \\\"）。和原始字符串字面量（Raw String Literals）用反引号 `` 包起来——不转义。', sections: [{ title: '字符串字面量', content: '解释型："hello\\nworld" → 包含换行符。原始型：\`hello\\nworld\` → 就是字面字符串 "hello\\nworld"（不转义）。原始型适合：正则表达式、JSON、多行字符串、Windows 路径。' }], diagnosis: [{ symptom: '原始字符串字面量末尾反斜杠报语法错误', cause: '原始字符串 \`...\` 中除了反引号外的字符都原样保留，但末尾的反斜杠会导致语法歧义。', fix: '末尾带 \\ 的字符串可改用双引号："C:\\path\\"。或用字符串拼接：\`C:\\path\` + "\\"。' }, { symptom: '解释型字符串中的未识别转义序列（如 \\d）', cause: '解释型字符串中 \\d 不是合法转义序列（Go 只认 \\n \\t \\\\ \\" \\r 等），未被识别的转义序列会被编译器报错或吞掉 \\ 保留原字符。', fix: '正则表达式等场景用原始字符串 \`...\` 避免转义地狱。解释型中需表示 \\ 本身需写 \\\\。' }], exercises: [{ level: '基础', task: '哪个字符串字面量适合写正则表达式', hint: '\\d 会被转义吗', answer: '原始型——\`\\d+\\.\\d+\` 不会转义反斜杠。解释型 "\\d+\\.\\d+" 中 \\d 变成了 d 字符——因为 \\d 不是有效转义序列所以 Go 报错。' }] },
  "j0w6e8Pksh30PKDvXj3yk": { mentalModel: 'Zero Value——Go 不给未初始化的变量留"undefined"，而是给一个"零值"：int=0、string=""、bool=false、pointer=nil、slice/map/chan=nil。没有"未定义"状态——所有变量都有确定值。', sections: [{ title: 'Zero Value 的安全性', content: '零值让 Go 代码更安全——不担心"使用未定义变量"。零值+命名返回让 return 不会忘。零值不是 nil panics 的根源——nil slice 可以 range（空切片）、nil map 读返回零值、nil map 写才 panic。' }], diagnosis: [{ symptom: '对 nil map 写入导致 panic', cause: 'var m map[string]int 声明的是 nil map，nil map 可读不可写。直接 m["key"] = 1 触发运行时 panic: assignment to entry in nil map。', fix: '使用前初始化：m := make(map[string]int) 或 m := map[string]int{}。struct 的 map 字段注意在构造函数中 make。' }, { symptom: 'json.Marshal 对 nil slice 和 empty slice 序列化不同', cause: 'nil slice 序列化为 null，empty slice（:= []int{} 或 make([]int, 0)）序列化为 []。API 响应中 null vs [] 可能影响前端解析。', fix: '统一为 empty slice：s := make([]int, 0) 或 s := []int{}。或文档明确说明 null 在响应中的含义。' }], exercises: [{ level: '基础', task: 'nil slice 和空 slice 有什么区别', hint: 'len/append', answer: 'var s []int → nil slice (len=0, s==nil)。s := []int{} → empty slice (len=0, s!=nil)。两者用 len/append 行为完全一样。唯一的区别是 JSON 序列化：nil → null、empty → []。' }] },
  "NfYlEY8f_YPITT6Exp3Iw": { mentalModel: 'Struct 指针——用 & 获取 struct 的指针、用 * 解引用。但 Go 让你通过指针直接访问字段（不用 -> 像 C 或 . 像 Java）——编译器自动解引用。', sections: [{ title: 'Pointers with Structs', content: 'p := &Person{Name:"Alice"} → p.Name = "Bob"（不用 (*p).Name）。这避免了 C 语言 -> 和 . 的混乱。new vs &: new(Person) 返回 *Person（零值初始化）；和 &Person{} 等价但后者更常用。指针接收者 vs 值接收者：指针接收者修改原值、避免大结构体拷贝。' }], diagnosis: [{ symptom: 'struct 字段未初始化导致 nil pointer dereference', cause: 'type Node struct { Next *Node }; n := Node{}; n.Next.Value 访问 nil 指针字段导致 panic。', fix: '在方法中检查 nil：if n.Next != nil { return n.Next.Value }。或提供构造函数初始化所有指针字段。' }, { symptom: '值接收者方法中修改 struct 但调用方未看到变化', cause: 'func (p Person) SetName(n string) { p.Name = n } 用值接收者，方法操作的是 Person 的副本而非原值。', fix: '改为指针接收者：func (p *Person) SetName(n string) { p.Name = n }。或返回新值：func (p Person) WithName(n string) Person { p.Name = n; return p }。' }], exercises: [{ level: '基础', task: 'Go 的 Struct 字段访问为什么可以省略 * 操作符', hint: '编译器自动处理', answer: 'Go 编译器看到 p.Name 而 p 是指针时自动解引用（等价 (*p).Name）。不需要像 C 那样手动 -> 或 (*p).Name。这样代码更干净。' }] },
  "P0fUxEbb3bVYg35VWyg8R": { mentalModel: '指针是"门牌号"——存的是变量的地址而不是值本身。& 取地址、* 解引用。Go 有指针但没有指针算术（不像 C 可以 p++ 乱指），比 C 安全得多。', sections: [{ title: 'Go 指针 vs C 指针', content: 'Go 指针安全限制：1) 不能做指针算术（p++ 不允许）2) 不能随意类型转换（unsafe.Pointer 可以但明确标记为 unsafe）3) 有 GC——指针不管理内存生命周期（不需要 free）。指针 vs 值传递：函数参数传值时拷贝整个 struct——大 struct 用指针传递节省拷贝。' }], diagnosis: [{ symptom: '取 map 元素的地址报错"cannot take address of map value"', cause: 'map 扩容时元素地址会变（元素可能被搬移到新位置），Go 禁止取 map 元素的地址。', fix: '用 map[string]*Value 存指针。或取出改完后写回：v := m[k]; v.Field = x; m[k] = v。切片元素可以取地址。' }, { symptom: '函数返回局部变量的指针，担心"悬空指针"', cause: '在 C 中返回栈变量的地址会导致"悬空指针"（dangling pointer）。Go 编译器通过逃逸分析检测到这种场景自动将该变量分配到堆上。', fix: 'func newInt() *int { x := 42; return &x } 是安全的。Go 编译器自动处理。不放心用 go build -gcflags="-m" 查看逃逸分析。' }], exercises: [{ level: '基础', task: '为什么 Go 禁止指针算术', hint: '安全', answer: '指针算术是 C/C++ 大量内存 bug（buffer overflow/use-after-free）的根源。Go 禁止它是为了内存安全——指针只能指向有效对象，不能随意移动到未知内存区域。' }] },
  "Eu8JV-_W-P_bCx_PglIW0": { mentalModel: '数组是"固定长度的连续存储"——[5]int 和 [3]int 是不同的类型（长度是类型的一部分）。数组用的不多——大多数场景用 slice（动态数组）。', sections: [{ title: 'Array vs Slice', content: '数组：[3]int{1,2,3}，长度不可变，赋值=复制整个数组。切片：[]int{1,2,3}，长度可变，赋值=复制引用（共享底层数组）。数组适用场景：固定大小集合（如 IPv4 地址 [4]byte）、编译时已知长度、需要值语义拷贝。' }], diagnosis: [{ symptom: '数组传给函数后函数内修改未影响原值', cause: 'Go 中数组是值类型，传递给函数时复制整个数组，函数内修改的是副本。这与 C 中数组退化为指针的行为完全不同。', fix: '传数组指针：func modify(a *[5]int) { a[0] = 999 }。但更推荐用切片：func modify(s []int) { s[0] = 999 }。' }, { symptom: '大数组赋值导致性能问题（复制 8MB）', cause: '[1000000]int 数组赋值时会发生完整的内存拷贝（8MB），与 C 区别巨大。', fix: '用切片替代：s := make([]int, 1000000) 赋值只复制 24 字节的 slice header。或用指针赋值：b := &a（复制 8 字节指针）。' }], exercises: [{ level: '基础', task: 'Go 中 [3]int 和 [5]int 是什么关系', hint: '类型系统', answer: '它们是不同的类型——不能互相赋值、不能比较。长度是数组类型的一部分。这和大多数其他语言不同（C 中 int[3] 可以隐式转换为指针）。' }] },
  "3aoMFQXIh3Qdo04isHwe_": { mentalModel: 'Slice 是 Go 最常用的数据结构——"动态数组视图"。slice 不存数据，只是指向底层数组的"窗口"（ptr+len+cap）。append/切片操作可能共享底层数组——这是最容易踩的坑。', sections: [{ title: 'Slice 内部原理', content: 'slice header = {ptr *T, len int, cap int}。make([]int, 5, 10) → len=5, cap=10。append 时 cap 不够→分配新底层数组（2x 扩容）。陷阱：s2 := s1[1:3] 共享底层数组→修改 s2 影响 s1。避免：用 copy(dst, src) 创建独立副本。' }], diagnosis: [{ symptom: '子切片 append 后意外修改了原切片可见范围外的元素', cause: 's2 := s1[1:3] 共享底层数组，且 s2 的 cap 延伸到原数组末尾。s2 = append(s2, x) 若 cap 够用会直接覆盖原数组的第 3 个元素。', fix: '用 full slice expression 限制 cap：s2 := s1[1:3:3] 使 s2 的 cap=2。或 copy 独立副本：s2 := make([]int, len(s1)); copy(s2, s1)。' }, { symptom: 'append 后忘记接收返回值导致数据丢失', cause: 's = append(s, x) 如果触发扩容，s 的 header（ptr）会变。直接 append(s, x) 而不接收返回值，扩容后丢失新数据。', fix: '始终 s = append(s, x)。go vet 会警告未使用 append 返回值的代码。' }], exercises: [{ level: '进阶', task: '解释为什么 append 可能产生意外副作用', hint: '共享底层数组', answer: 's2 := s1[:2]; s2 = append(s2, 100) → 如果 cap 还有空间，append 写入底层数组位置 2→修改了 s1 看不到的元素。但其实 s1 的第三个元素被改了。用 s2 := make([]int, len(s1)); copy(s2, s1) 创建独立副本。' }] },
  "PWrfmYnOJRlwgdFat91bC": { mentalModel: 'make() 是 slice/map/chan 的专属初始化——分配底层数据结构并返回初始化好的值。new() 只分配零值内存返回指针。slice/map/chan 必须用 make。', sections: [{ title: 'make vs new', content: 'make([]int, 5, 10) → 分配底层数组并返回 slice（len=5, cap=10）。make(map[string]int) → 分配哈希表并返回 map。make(chan int, 10) → 分配缓冲区并返回 channel。new(T) → 分配零值 T 并返回 *T。slice/map/chan 用 new 返回的是 *nil slice/*nil map——nil map 不能写！所以必须用 make。' }], diagnosis: [{ symptom: '用 new(map[string]int) 创建 map 后写入 panic', cause: 'new(map[string]int) 返回 *map[string]int，指向 nil map。nil map 写入会 panic。', fix: '用 make(map[string]int) 创建。或 m := map[string]int{}。new 只分配指针，不分配内部数据结构。' }, { symptom: 'make slice 时 len 和 cap 参数混淆', cause: 'make([]int, 5) 创建 len=5 cap=5 的 slice（5 个零值元素）。make([]int, 0, 5) 创建 len=0 cap=5 的空 slice。前者 append 在第 6 个位置，后者在第 1 个位置。', fix: 'make([]int, 0, capacity) 预分配容量但保持空。用 append 填充。区分"预分配有多少初始元素"(len)和"底层数组能容纳多少"(cap)。' }], exercises: [{ level: '基础', task: '为什么 map 必须用 make 初始化', hint: 'nil map', answer: 'var m map[string]int → nil map。m["key"] = 1 → panic: assignment to entry in nil map。make(map[string]int) 分配哈希表结构。但 nil map 可以读（返回零值）、可以 range（空循环）。' }] },
  "Q6ic-AGG_gnXEOdmqom89": { mentalModel: '遍历 Map——用 for k, v := range m。Map 的遍历顺序是故意随机化的（防滥用顺序依赖）。只遍历 key：for k := range m。只取 value：for _, v := range m。', sections: [{ title: 'Map 遍历注意', content: '遍历前检查 nil map：range 在 nil map 上不 panic（空迭代）。并发读 map 安全、并发读写 panic。需要并发访问→sync.Map 或 sync.RWMutex。遍历中删除元素是安全的。' }], diagnosis: [{ symptom: 'fatal error: concurrent map read and map write', cause: '一个 goroutine 在遍历 map，另一个 goroutine 在写 map。Go 运行时检测到并发读写冲突直接 fatal error（不是 panic，不可 recover）。', fix: '用 sync.RWMutex：读前 RLock()，写前 Lock()。或用 sync.Map（适合读多写少）。或用 channel 统一由单 goroutine 操作 map。' }, { symptom: 'map 遍历顺序每次运行都不同', cause: 'Go 故意在 map 遍历开始时随机化起始位置，防止开发者依赖遍历顺序。这不是 bug 是有意的设计。', fix: '需要有序遍历：收集 keys 到切片→排序→按排序后的 keys 依次访问：keys := make([]string, 0, len(m)); for k := range m { keys = append(keys, k) }; sort.Strings(keys)。' }], exercises: [{ level: '基础', task: '为什么 Go 的 map 遍历顺序是随机的', hint: '安全', answer: 'Go 故意在遍历开始时随机化起始位置——防止开发者依赖遍历顺序（map 本质是无序的）。如果开发者依赖了遍历顺序而 Go 改了实现→代码崩溃。随机化提前暴露这种依赖。' }] },
  "2IFxz3sFqDvUvzWNVwlWC": { mentalModel: '类型转换（Type Conversion）是"数据换包装"——int(x) 把 x 从一种类型转成另一种。和类型断言（Type Assertion）不同——转换在兼容类型间进行（int→float），断言在 interface 类型内操作。', sections: [{ title: 'Type Conversion 规则', content: '数值转换：int(3.14)→3（截断不是四舍五入）。string(65)→"A"（Unicode code point 65=字母 A）。[]byte("hello") 和 string([]byte{104,101,108,108,111}) 双向转换。strconv 包做字符串→数值的转换：strconv.Atoi("42")。' }], diagnosis: [{ symptom: 'int(3.14) 结果是 3 而非 4（期望四舍五入）', cause: 'Go 类型转换直接截断小数部分，与 math.Round 行为不同。', fix: '需要四舍五入：int(math.Round(3.14))。需要向上取整：int(math.Ceil(3.14))。需要向下取整：int(math.Floor(3.14))。' }, { symptom: 'int8(256) 编译报错"constant 256 overflows int8"', cause: 'int8 范围是 -128 到 127，常量 256 超出 int8 范围，Go 编译器在编译时检测常量溢出。', fix: '用 int16(256) 或 int(256)。运行时转换不会检查溢出（如 int8(someVar) 静默溢出）。需要范围检查：if v > math.MaxInt8 { ... }。' }], exercises: [{ level: '基础', task: 'int(3.9) 的结果是多少', hint: '截断', answer: '3——Go 的类型转换截断小数部分（不四舍五入）。和 math.Round(3.9)→4 不同。截断是直接丢弃小数。' }] },
  "BRKryB5HawXyXeBWM4QqU": { mentalModel: 'continue 跳过循环中剩余代码进入下一次迭代——"这遍不过瘾，直接下遍"。和 break 的区别：break 直接退出循环，continue 只是跳过本轮。', sections: [{ title: 'continue 的高级用法', content: 'for i, v := range items { if v == "" { continue }; process(v) }——跳过空元素。带标签的 continue：outer: for ... { for ... { continue outer } }——跳到外层循环的下一次迭代。continue 比 if-else 嵌套更干净——"不符合条件就跳过"而非"符合条件才执行"。' }], diagnosis: [{ symptom: '带标签的 continue 跳转错误层级的循环', cause: 'continue outer 跳转到 outer 标签标记的外层循环的下一次迭代，而不是继续内层循环。容易在嵌套循环中混淆。', fix: '明确标签名称和期望的跳转目标。内层 continue（无标签）只影响内层循环。带标签前确认跳转目标。' }, { symptom: 'continue 后忘记更新迭代条件导致无限循环', cause: 'for i := 0; i < n; i++ { if cond { continue }; ... } 中 continue 跳过了 i++ 但 for 语句的 post statement（i++）在每次循环体执行完后自动执行——这通常没问题。但 while 风格的 for（for i < n { i++; ... }）中 continue 可能跳过 i++ 导致无限循环。', fix: 'while 风格中注意 continue 前手动更新条件：for i := 0; i < n; { if cond { i++; continue }; process(); i++ }。或用 for range 避免手动管理。' }], exercises: [{ level: '基础', task: '用 continue 改写：if 和 else 深层嵌套的过滤循环', hint: '提前 continue 跳过不符合条件的', answer: 'for _, item := range items { if item.Deleted { continue }; if !item.Active { continue }; if item.Price <= 0 { continue }; process(item) }——Guard Clause 模式，比深层 if-else 清晰。' }] },
  "IWdAJ1BXqJv8EMYvFWRaH": { mentalModel: 'break 退出最内层循环——"够了，我不想再跑了"。带标签：break outer 退出外层循环——break 和 continue 都支持标签。', sections: [{ title: 'break 标签用法', content: 'outer: for i := 0; i < 10; i++ { for j := 0; j < 10; j++ { if matrix[i][j] == target { break outer } } }——break outer 退出外层循环而不是内层循环。switch/select 中的 break 只退出 switch/select 而非外层循环——这也是标签的用武之地。' }], diagnosis: [{ symptom: 'for 循环中的 switch/select 内 break 只退出 switch/select', cause: 'for { switch x { case 1: break } } 中 break 只退出 switch，不退出 for 循环。这是 Go 与 C 的重要区别。', fix: '要退出 for 需用标签：loop: for { switch x { case 1: break loop } }。或用 return 退出整个函数。或用 goto（不推荐）。' }, { symptom: 'select 中 break 行为与直觉不符', cause: 'for { select { case <-ch: if done { break } } } 中的 break 只跳出 select，不跳出 for。', fix: '用带标签的 break：loop: for { select { case <-ch: if done { break loop } } }。或将 break 条件设为标志变量。' }], exercises: [{ level: '基础', task: 'for 循环中的 switch；break 退出的是 switch 还是 for', hint: 'break 退出最内层', answer: 'for ... { switch ... { case ...: break } }——break 只退出 switch，不退出 for 循环。要退出 for 循环需要标签：outer: for ... { switch ... { case ...: break outer } }。' }] },
  "O29VoTfPiU8GZ_c16ZJIp": { mentalModel: 'goto 是 Go 的"禁忌魔法"——能跳到同一函数内任意位置。几乎所有场景有更好的替代（函数/循环/条件），但极少数情况（错误处理、状态机）goto 反而是最干净的。', sections: [{ title: 'goto 的正确用法', content: 'Go 的 goto 有限制：不能跳过变量声明、不能跳进其他块。极少数正当用法：统一错误处理出口（goto cleanup）、状态机跳转。99% 的情况不需要。Go 保留它是为了"某些情况真的需要"。不要用 goto 写常规循环和控制流——用 for/if/func。' }], diagnosis: [{ symptom: 'goto 跳过变量声明导致编译错误', cause: 'goto target: 不能跳过变量声明语句。Go 编译器会报"goto X jumps over declaration of Y"。', fix: '确保 goto 的目标位置不跨越变量声明。如果必须跳过，将被跳过的变量声明移到 goto 之前。或用函数封装。' }, { symptom: 'goto 跳入其他代码块导致编译错误', cause: 'goto 不能跳进变量作用域（如跳入 if/for/switch 代码块内）。Go 编译器会报"goto X jumps into block"。', fix: 'goto 只能跳到同一层或外层。跳入内层代码块在 Go 是不允许的。用 defer/return 或函数封装替代。' }], exercises: [{ level: '基础', task: 'Go 的 goto 和 C 的 goto 有什么关系', hint: '更严格', answer: 'Go 的 goto 比 C 更安全——不能跨变量声明（编译错误）、不能跳进另一个代码块。这些限制防止了 C 中最恶心的 goto 滥用模式。' }] },
  "MhvDlY3HE9tmAki7lOOh0": { mentalModel: 'Struct Tags 是写在 struct 字段后面反引号里的元数据——``。最常用于 JSON 序列化（json:"name"）和数据库映射（db:"id"），也可用于自定义校验、文档生成。', sections: [{ title: 'Struct Tags 详解', content: 'JSON 标签：Name string `json:"name,omitempty"`——omitempty 表示零值时忽略字段。- 标签：`json:"-"` 表示永远不序列化该字段。多重标签：`json:"id" db:"user_id"`——空格分隔不同用途的标签。自定义标签通过 reflect 包解析——ORM/validator 库大量使用。' }], diagnosis: [{ symptom: 'struct tag 语法错误未被编译时发现', cause: 'struct tag 是字符串字面量，Go 编译器不验证 tag 内容。写错如 json:"name,omitempty" 缺少反引号或空格位置错误只能在运行时才发现。', fix: '用 go vet 检查 struct tag 格式（Go 1.15+）。用静态分析工具。编写 tag 时仔细检查拼写和逗号位置。' }, { symptom: 'omitempty 导致 int=0 字段被忽略但 0 是有效业务值', cause: 'omitempty 对数值类型把零值当作"空"省略。int 的 0、float64 的 0.0、bool 的 false 都会被省略。', fix: '用指针：*int `json:"score,omitempty"`。nil 指针省略，非 nil（即使值为 0）会序列化。或用自定义 MarshalJSON 方法。' }], exercises: [{ level: '基础', task: 'omitempty 在 struct 序列化为 JSON 时的作用', hint: '零值', answer: 'type User struct { Name string `json:"name,omitempty"` }。如果 Name=""→序列化后的 JSON 不含 name 字段。不用 omitempty→"name":""。' }] },
  "NbfndS8CBs7wiUckv6pQO": { mentalModel: 'Struct 方法是"挂载在类型上的函数"——func (u User) Greet() 不是 func Greet(u User)。方法比函数多了接收者（receiver）。值接收者 vs 指针接收者是最重要的选择。', sections: [{ title: '方法 vs 函数', content: '值接收者：(u User) → 不能修改原值、每次调用拷贝 struct。指针接收者：(u *User) → 修改原值、不拷贝。方法可以定义在任意本地类型（type MyInt int），但不能给外部包的类型定义方法。Go 没有 class——struct+方法就是对象。' }], diagnosis: [{ symptom: '方法用指针接收者但接口要求值接收者导致接口不满足', cause: '指针接收者方法（*T）只属于 *T 类型。值接收者方法同时属于 T 和 *T。所以有 *T 方法的类型不满足需要值接收者的接口。', fix: '如果类型需要同时满足接口，值接收者方法确保 T 和 *T 都满足。不可变类型用小 struct 值接收者。需要修改状态用指针接收者+传指针给接口。' }, { symptom: '给非本包类型定义方法编译报错', cause: 'Go 不允许给其他包定义的类型（包括内置类型如 int、string）定义方法。编译器报"cannot define new methods on non-local type"。', fix: '创建自定义类型：type MyInt int，然后在 MyInt 上定义方法。或用包装器：type Wrapper struct { external.Type }。' }], exercises: [{ level: '基础', task: '什么时候用值接收者、什么时候用指针接收者', hint: '修改/拷贝成本', answer: '需要修改接收者值→指针。接收者是大型 struct→指针（减少拷贝）。接收者是 slice/map/chan→值（它们本身就是引用）。类型是基础类型或小型 struct 且不需要修改→值。' }] },
};