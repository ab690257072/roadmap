import type { DeepTutorial } from '../deep-types';

export const GOLANG_DEEP_TUTORIALS_9: Record<string, DeepTutorial> = {
  "QWixUXjC8YG-i1gsKVY1v": { mentalModel: 'Zero Values——Go 不给未初始化变量留"未定义"，所有类型都有一个明确的零值。int=0, string="", bool=false, pointer=nil, slice/map/chan=nil。这避免了"未定义行为"。', sections: [{ title: '零值的作用', content: '零值让 Go 代码更可预测。var i int 后 i 一定是 0 不存在随机内存值。零值+命名返回=naked return 安全。nil slice 可以 append 和 range（和空切片行为一样）。nil map 读返回零值（不 panic）但写 panic。' }], diagnosis: [{ symptom: 'nil map 写入 panic 但 nil slice append 正常', cause: 'nil map 缺少底层哈希表结构，写入需要分配 bucket。nil slice 没有底层数组，但 append 会自动分配。Go 设计者选择让 nil slice 更实用但 nil map 更安全（显式初始化）。', fix: '初始化 map：m := make(map[string]int)。检查 map 是否为 nil：if m == nil { m = make(map[string]int) }。' }, { symptom: 'nil pointer dereference 在 struct 链式调用中', cause: 'type Node struct{ Next *Node }; n.Next.Value 中 Next 为零值 nil，解引用导致 panic。', fix: '链式访问前检查 nil：if n.Next != nil { return n.Next.Value }。提供构造函数安全初始化指针字段。使用 Option 模式逐步构建对象。' }], exercises: [{ level: '基础', task: 'nil map 和空 map 的读写区别', hint: '只有写才 panic', answer: 'var m map[string]int → nil map。m["a"]=1 → panic（assignment to entry in nil map）。_ = m["a"] → 0（读返回零值不 panic）。empty := map[string]int{} → 读写都安全。' }] },
  "SW2uMlhfC-dnQakShs2km": { mentalModel: 'Struct Tags——元数据标签写在 struct 字段后面反引号 `json:"name"`。JSON 序列化用 json tag，数据库用 db tag，校验用 validate tag。', sections: [{ title: 'Tag 解析', content: 'tag 格式：`json:"fieldname,omitempty" db:"col"`。reflect 包读取：field.Tag.Get("json")。常用 tag：json（序列化）、db（数据库列名）、validate（校验规则）、yaml（YAML 序列化）。tag 是字符串——解析由库负责（encoding/json 看 json tag，gorm 看 gorm tag）。' }], diagnosis: [{ symptom: 'json tag 写成了 jason 或不注意拼写错误', cause: 'struct tag 是字符串，Go 编译器不做语义验证。拼写错误的 tag（如 jsno:"name"）在运行时才表现为字段名不符合预期。', fix: '使用 IDE 的 struct tag 自动补全和校验。go vet 可检某些 struct tag 错误。单元测试验证序列化结果。' }, { symptom: '多重 tag 格式错误（空格与分号混淆）', cause: '多个 tag 用空格分隔：`json:"name" xml:"name"`。如果 tag 值中有空格需用引号括起整个值。格式错误导致 reflect 解析返回空字符串。', fix: '标准格式：key:"value" 键值对，多个之间空格分隔。示例：`protobuf:"varint,1,opt,name=foo" json:"foo,omitempty"`。用 go vet 验证 tag 格式。' }], exercises: [{ level: '基础', task: '写一个 struct，字段用 json tag 控制序列化名称和 omitempty', hint: '\`json:"name,omitempty"\`', answer: 'type User struct { ID int `json:"id"`; Name string `json:"name,omitempty"`; Password string `json:"-"` }——json:"-" 永远不序列化 Password 字段。' }] },
  "XupDuVRsM5VjdaNs7JaT5": { mentalModel: 'Go 的并发原语——goroutine+channel+select+sync。不是"用库实现并发"，并发是语言特性。go 关键字启动 goroutine，chan 通信，"不要通过共享内存通信，要通过通信来共享内存"。', sections: [{ title: 'CSP 并发模型', content: 'CSP（Communicating Sequential Processes）——Go 的并发哲学。goroutine 是"轻量线程"（2KB 栈→可创建百万个）。channel 是"通信管道"（有缓冲/无缓冲）。select 是"多路监听"（哪个 channel 有数据就处理哪个）。sync 包提供传统同步原语（Mutex/WaitGroup/Once）。原则：优先 channel，必要时 sync。' }], diagnosis: [{ symptom: 'goroutine 泄漏——goroutine 数量持续增长不下降', cause: 'goroutine 在 channel 上阻塞等待永远不会到达的消息，或死循环没有退出条件。', fix: '用 context 传播取消信号。select 中加入 <-ctx.Done()。用 pprof 的 goroutine profile 定位泄漏源。runtime.NumGoroutine() 监控。' }, { symptom: 'channel 关闭后继续发送导致 panic', cause: '一个已经 close 的 channel 再 send 会触发 panic: send on closed channel。', fix: '由发送方负责 close channel。确保所有发送完成后才 close。用 sync.Once 保护 close 避免重复关闭。接收方判断：v, ok := <-ch。' }], exercises: [{ level: '基础', task: 'Go 的并发名言是什么', hint: '"不要用...通信，要用通信..."', answer: '"Do not communicate by sharing memory; instead, share memory by communicating."——不要通过共享内存来通信，而要通过通信来共享内存。' }] },
  "ZmPhiTrB8EK0LDdaJOhc8": { mentalModel: 'Go Module——go mod 是依赖管理工具。go.mod 文件记录模块名+Go 版本+依赖。go.sum 记录依赖的哈希值（防篡改）。', sections: [{ title: 'go mod 命令', content: 'go mod init module-name：初始化。go mod tidy：移除未用依赖、添加缺失的。go get pkg@v1.2.3：安装指定版本。go mod download：下载依赖到本地缓存。go mod vendor：复制依赖到 vendor/ 目录。语义化版本：v1.2.3——v1 主版本、v2+ 需要更新模块路径（github.com/me/proj/v2）。' }], diagnosis: [{ symptom: '发布了 v2 但 import 路径还是旧路径（不含 /v2）', cause: 'Go 模块约定中 v2+ 主版本需要在 module path 中包含版本后缀，否则 go get 会下载到错误版本。', fix: 'go.mod: module github.com/user/proj/v2。import 同步更新：import "github.com/user/proj/v2"。tag 打 v2.0.0。' }, { symptom: 'go.sum 冲突——同一个包出现多个不同的哈希值', cause: '多个 go.mod 文件（如 monorepo）引用了同一依赖的不同版本，或依赖被恶意篡改。', fix: 'go mod tidy 清理。检查是否有多个 go.mod。私有模块设置 GOPRIVATE。用 GONOSUMCHECK 跳过校验（仅限信任源）。' }], exercises: [{ level: '基础', task: 'go.sum 文件的作用', hint: '安全', answer: 'go.sum 记录每个依赖的 SHA-256 哈希。每次下载依赖时 Go 验证——如果哈希不匹配（依赖包被篡改/删除），Go 拒绝使用。保护供应链安全。' }] },
  "ZxZpReGdPq8ziSlYmf2nj": { mentalModel: 'Go 没有异常（try/catch）——用 error 返回值表达失败。panic/recover 是"真正不可恢复的错误"（如数组越界、nil map 写）。不要用 panic 替代 error。', sections: [{ title: 'Error vs Panic', content: 'error：可预期的失败（文件不存在、网络超时、业务校验不通过）→ 返回 error。panic：不应该发生的情况（程序 Bug——nil pointer dereference、数组越界、写入 nil map）→ crash 并输出 stack trace。recover：defer 中调用 recover() 捕获 panic，让程序不 crash 但已经是不正常状态——只用于顶层错误处理和测试。' }], diagnosis: [{ symptom: '库代码中使用 panic 作为常规错误处理', cause: '库函数调用 panic 会让调用方的程序崩溃，调用方无法优雅处理。', fix: '库代码永远返回 error 而非 panic。只有 Must* 模式（main 包初始化）允许 panic。标准库的 MustCompile 仅在 init/全局变量赋值时使用。' }, { symptom: 'recover 在非 defer 直接调用的函数中返回 nil', cause: 'recover 必须在 defer 调用的函数中直接或间接执行才有效。defer recover()（无外层函数调用）不生效。', fix: '正确写法：defer func() { if r := recover(); r != nil { ... } }()。将 recover 包在 func() { ... }() 中。' }], exercises: [{ level: '基础', task: '什么时候该 panic、什么时候该返回 error', hint: '可预期 vs Bug', answer: '返回 error：文件不存在、网络超时、用户输入非法——这些是"正常业务流程"。panic：数组越界、nil map 写——这些是"程序员的 Bug"。永远不要 panic 替代 error return。' }] },
  "qoQ6_5MgTu3ur1mkEgqG5": { mentalModel: 'Go 的工具链——不只是编译器，是完整的开发工具集。go build、go test、go fmt、go vet、go mod、go doc——Go 认为"工具也是语言的一部分"。', sections: [{ title: 'Go 工具链', content: 'go build：编译。go test：测试。go fmt：格式化。go vet：静态分析（发现可疑代码）。go mod：依赖管理。go doc：查看文档（go doc fmt.Println）。go generate：代码生成。go tool pprof：性能分析。go run：编译+运行。gopls：LSP 语言服务器（IDE 支持）。所有工具都内置——不需要选构建系统（make/cmake/npm/gradle）。' }], diagnosis: [{ symptom: 'go vet 报错"Printf format %d has wrong type"', cause: 'fmt.Printf 的格式化动词与参数类型不匹配。%d 需要 int 但传入了 string。', fix: '检查 Printf/Sprintf/Fprintf 的格式化动词与参数类型一致。%s=string, %d=int, %f=float64, %v=any, %+v=含字段名。用 go vet 自动检测。' }, { symptom: 'go doc 显示"package ... is not in GOROOT"', cause: 'go doc 默认只查标准库和当前模块的包。第三方包未下载或不在当前模块中。', fix: '先 go get 下载第三方包。或 go build ./... 后 go doc。或用 pkg.go.dev 在线查看。go doc -src 查看有源码的包。' }], exercises: [{ level: '基础', task: 'go vet 和 go fmt 有什么区别', hint: '格式 vs 逻辑', answer: 'go fmt 只处理格式（缩进/空格/换行）。go vet 做静态分析检测可疑代码——如 unreachable code、Printf 参数不匹配、锁的错误使用。' }] },
  "cEQ9NQX7ZkKLwz_hg9L_7": { mentalModel: 'Defer——函数返回前执行的"收拾"代码。像写了一个待办清单：打开文件时 defer file.Close()、获取锁时 defer mu.Unlock()。按 LIFO 顺序执行（后 defer 的先执行）。', sections: [{ title: 'Defer 的执行机制', content: 'defer 在函数返回时执行（即使 panic）。多个 defer→后进先出（栈）。defer 的参数在 defer 语句执行时求值（不是 defer 执行时）——defer fmt.Println(i); i++ → 打印的是 i 的旧值。defer 是最重要的 Go 习惯——确保资源释放，避免忘记 Close/Unlock。' }], diagnosis: [{ symptom: '循环中 defer 累积导致内存泄漏', cause: 'for { defer f.Close() } 中 defer 不会在每次迭代结束时执行，而是等到函数返回时才执行。循环内大量 defer 不执行，既堆积资源又耗内存。', fix: '不要在循环内使用 defer。将循环体提取为独立函数，在内部 defer。或手动在循环末 Close。' }, { symptom: 'defer 闭包捕获循环变量取到错误值', cause: 'Go 1.21 之前：for i := range items { defer func() { fmt.Println(i) }() } 所有 defer 闭包引用同一个循环变量，取最终值。', fix: 'Go 1.22+ 自动为每次迭代创建新变量。旧版：i := i（shadow）后 defer，或作为参数传入：defer func(j int) { ... }(i)。' }], exercises: [{ level: '基础', task: '解释 defer 的 LIFO 执行顺序', hint: '栈', answer: 'defer A(); defer B(); defer C() → 执行顺序 C, B, A。因为每个 defer 压入栈，函数返回时从栈顶弹出执行。这个设计让资源释放顺序和获取顺序相反（后获取的先释放）。' }] },
  "dMdOz2kUc8If3LcLZEfYf": { mentalModel: 'Go 的 data race（数据竞争）——两个 goroutine 同时读写同一个变量且至少一个是写。Go 的 race detector（go test -race）能在测试时检测出来。', sections: [{ title: 'Race Detector', content: 'go test -race：编译并运行测试，检测 data race。go run -race：开发时本地检测。原理：记录每次内存访问的 goroutine ID 和时间戳→检测冲突访问。代价：内存增加 5-10x、速度慢 10-20x——仅用于测试/开发。CI 中跑 -race 很有价值——提前发现并发 bug。' }], diagnosis: [{ symptom: '-race 偶尔能检测到但有时又检测不到', cause: 'race detector 只能检测实际发生的并发冲突。如果时序从未对撞，race 不可见——这并不意味着没有 race。', fix: '即使 -race 没报告也可能存在潜在 race。多次运行 -race（go test -race -count=10）。Code review 并发逻辑。用 stress 工具压力测试（go test -race -count=100）。' }, { symptom: '-race 报告假阴性——测试通过但生产 crash', cause: 'race detector 检测需要冲突实际发生。低概率时序冲突在测试中可能永远不出现。', fix: 'CI 并行跑 -race（go test -race -count=100 -shuffle=on）。生产用 read-only race detection（asan 方式）。无法检测的单例模式隐患手动 review。' }], exercises: [{ level: '基础', task: 'data race 和 race condition 区别', hint: '一个一定是 bug，一个可能是 bug', answer: 'Data race：多个 goroutine 同时读写同一内存（没有同步）→ 一定是 bug（未定义行为）。Race condition：逻辑上的时序依赖（如先检查后操作——check-then-act）→ 使用同步原语也可能有 race condition。Data race 是"底层的内存冲突"，Race condition 是"高层逻辑的时序依赖"。' }] },
  "ijS-b400c9BldRO6PlrC1": { mentalModel: 'Go Error Wrapping——fmt.Errorf("%w", err) 保留原始 error 链。errors.Is/As 沿链查找。', sections: [{ title: 'Error Wrapping', content: '包装：if err != nil { return fmt.Errorf("do X: %w", err) }。errors.Is(err, sql.ErrNoRows) 检查链中是否有特定 error。errors.As(err, &target) 提取链中特定类型 error。不要用字符串比较判断 error 类型。' }], diagnosis: [{ symptom: '用 %v 包装错误后用 errors.Is 找不到原始错误', cause: 'fmt.Errorf("ctx: %v", err) 只做字符串格式化，不保留错误链。errors.Is 无法通过 %v 跟踪到原始错误。', fix: '始终用 %w 包装错误：fmt.Errorf("ctx: %w", err)。一个 fmt.Errorf 中最多一个 %w。Go 1.20+ 支持 errors.Join 组合多个错误。' }, { symptom: 'errors.Is(err, sentinel) 返回 true 但 err.Error() 不包含 sentinel 的消息', cause: 'errors.Is 沿链比较错误值，找到后返回 true。但 Error() 只显示最外层错误信息。哨兵错误被深埋在链中。', fix: '正常行为——不是 bug。需要显示完整链：用 %+v 或迭代 errors.Unwrap 构建完整消息。fmt.Println(err) 只显示最外层。' }], exercises: [{ level: '基础', task: 'errors.Is vs == 比较', hint: '链', answer: 'err == io.EOF 只能判断最外层 error。errors.Is(err, io.EOF) 沿着 wrap 链查找，即使 error 被 fmt.Errorf 包装了多层也能匹配。生产代码应该用 errors.Is。' }] },
  "j0zmfpaufr5TAZa_Bh8Vu": { mentalModel: 'Go Tool——go tool 下的一系列底层工具。go tool compile（编译）、go tool link（链接）、go tool pprof（性能分析）、go tool trace（追踪分析）。', sections: [{ title: 'go tool 工具集', content: 'go tool pprof：分析 CPU/内存 profile。go tool trace：goroutine 调度和执行可视化。go tool objdump：反汇编。go tool cover：生成覆盖率报告。大部分用 go 一级子命令就够了，go tool 是给深入调试准备的。' }], diagnosis: [{ symptom: 'go tool pprof 打开 profile 文件报"unrecognized profile format"', cause: 'profile 文件可能被截断（采集时间不足），或用不同 Go 版本生成。', fix: '确保采样时间足够（CPU profile 30s+）。用相同 Go 版本打开。检查文件是否完整：go tool pprof -raw profile.out。重新采集。' }, { symptom: 'go tool trace 文件过大无法打开（几百 MB）', cause: 'trace 记录所有 goroutine 调度、GC、系统调用等事件，长采样产生巨大文件。', fix: '采样时间限制在 3-5 秒。用 runtime/trace.SetMaxEvents 限制事件数量。针对特定问题短采样。生产环境用 pprof 替代 trace 做长期监控。' }], exercises: [{ level: '基础', task: 'go tool pprof 分析 CPU profile', hint: 'go test -cpuprofile', answer: 'go test -cpuprofile=cpu.out -bench=. → go tool pprof cpu.out → top 看热点函数 → web 看火焰图。' }] },
  "jx1vln8hqRJqVZfJi6CST": { mentalModel: 'go:generate 是代码生成指令——在 Go 文件中写 //go:generate command args，然后运行 go generate 自动执行这些命令。用来生成 mock/序列化/枚举代码。', sections: [{ title: 'go generate 用法', content: '//go:generate stringer -type=Status。//go:generate mockgen -source=interface.go。go generate ./... 遍历所有包执行 generate 指令。和 make 不同——generate 指令和代码放在一起（维护代码的人就知道怎么重新生成）。' }], diagnosis: [{ symptom: 'go generate 执行时报"command not found"', cause: '代码生成工具未安装或不在 PATH 中。go install 安装的工具可能未在 PATH。', fix: 'go install tool@latest。检查 GOBIN 是否在 PATH：export PATH=$PATH:$(go env GOPATH)/bin。或用 go run 替代：//go:generate go run gen.go。' }, { symptom: 'go generate 生成的文件与实际代码不同步', cause: '修改了接口后忘记重新运行 go generate，或 CI 中未验证生成文件是否过时。', fix: '在 CI 中检查：go generate ./... && git diff --exit-code。如果生成文件与代码不一致则 CI 失败。Makefile 中 build 前执行 generate。' }], exercises: [{ level: '基础', task: 'go generate 和 Makefile 的区别', hint: '位置', answer: 'go generate 指令写在 Go 源文件中——和生成目标代码在同一文件。Makefile 单独文件管理。generate 更"自包含"——开发者读到代码就看得到 generate 指令。' }] },
  "naBjKGrTBzlpapXzLHfvG": { mentalModel: 'Go 的内存管理——有 GC（垃圾回收）但和 Java 不同：没有 JVM、编译成二进制、GC 短暂停顿。理解栈 vs 堆、逃逸分析能写出更高效的 Go 代码。', sections: [{ title: 'Go GC 特点', content: '并发标记-清除、非分代 GC。GC pause 极短（<100μs 通常）。GOGC 环境变量控制 GC 频率（默认 100=堆翻倍触发）。逃逸分析：编译器自动判断变量分配在栈还是堆——go build -gcflags="-m" 查看逃逸分析结果。小对象优先栈分配（函数返回时直接释放）。大对象/跨函数引用的→堆分配。' }], diagnosis: [{ symptom: 'pprof heap 显示内存持续增长但未 OOM', cause: 'goroutine 泄漏是最常见原因——goroutine 持有引用阻止 GC 回收。或 slice 引用大数组前缀导致整个大数组无法被回收。', fix: 'pprof 检查 goroutine 数量。小切片用 copy：s := make([]T, n); copy(s, largeSlice[:n])。大切片及时置 nil 释放底层数组。sync.Pool 复用对象减少分配。' }, { symptom: 'GC 延迟毛刺——偶尔出现长 STW（Stop The World）', cause: '大堆对象导致标记/清除时间增加。大量指针对象导致写屏障开销。', fix: '减少堆分配（值类型替代指针）。sync.Pool 复用频繁创建的对象。调高 GOGC 减少 GC 频率。GOMEMLIMIT 设置软内存上限让 GC 更积极在接近限制时运行。' }], exercises: [{ level: '进阶', task: '用 escape analysis 分析一个变量分配在栈还是堆', hint: 'go build -gcflags="-m"', answer: 'go build -gcflags="-m" 输出中 "moved to heap" 表示变量逃逸到堆、"does not escape" 表示在栈上。' }] },
  "nzPe6XVqhtOZFbea6f2Hg": { mentalModel: 'Select 是 goroutine 的多路复用器——同时监听多个 channel，哪个先有数据就先处理哪个。相当于"同时等 3 个电话，哪个先响接哪个"。', sections: [{ title: 'Select 模式', content: 'select { case msg1 := <-ch1: ... case msg2 := <-ch2: ... case <-timeout: ... default: ... }。default 是非阻塞——所有 channel 都没数据时执行 default（不等待）。没有 default 的 select 会阻塞。随机选择——多个 case 同时就绪→随机选一个（公平分配）。' }], diagnosis: [{ symptom: 'select 中 time.After 在循环中导致 timer 泄漏', cause: '每次 for { select { case <-time.After(d): ... } } 创建新的 timer。time.After 返回的 timer 在过期前不会被 GC，循环中快速积累导致内存泄漏。', fix: '循环外创建 timer 并 Reset：t := time.NewTimer(d); for { t.Reset(d); select { case <-t.C: ... } }。或用 context.WithTimeout 替代。' }, { symptom: '带 default 的 select 让 CPU 持续 100%', cause: 'for { select { case msg := <-ch: ...; default: ... } } 中如果 ch 为空，default 立即执行（非阻塞），形成 busy loop 持续消耗 CPU。', fix: '不要在 for-select 中无限制使用 default。需要非阻塞检查时加 sleep：default: time.Sleep(time.Millisecond)。或用 select 无 default 阻塞等待。' }], exercises: [{ level: '基础', task: 'select 和 switch 有什么相似和不同', hint: '阻塞', answer: '相似：都是"多路选择"。不同：select 只用于 channel 操作、case 随机选择（同时就绪时）、可以阻塞（没有 default）、本质是通信而非条件判断。' }] },
  "oAvCO3GOKktkbmPahkPlT": { mentalModel: 'Go 的 GC（垃圾回收）——不需要手动 free，编译器自动判断变量生命周期。GOGC 调 GC 频率，值越大 GC 越不频繁但内存占用越大。', sections: [{ title: 'GC 调优', content: 'GOGC=100（默认）：堆内存使用量达到上次 GC 后存活量的 2 倍时触发 GC。GOGC=off：关闭 GC（适合极端性能场景）。GOMEMLIMIT：软内存限制（GC 更积极）。查看 GC 日志：GODEBUG=gctrace=1 启动程序。调优策略：减少分配（复用对象、sync.Pool）、减少指针（更多栈分配）。' }], diagnosis: [{ symptom: 'GOGC=off 后内存使用持续增长最终 OOM', cause: '关闭 GC 后堆内存只增不减，如果程序持续分配对象，最终耗尽可用内存。', fix: 'GOGC=off 仅在确认分配模式可控时使用（如启动后几乎不分配）。配合 GOMEMLIMIT 设软上限。用 GODEBUG=gctrace=1 监控分配模式后再调 GC。' }, { symptom: 'GC 日志中 gc 次数频繁（秒级多次 GC）', cause: 'GOGC 设置过低，或程序分配速率极高。每次 GC 扫描大堆浪费 CPU。', fix: '调高 GOGC（如 200 或 500）。用 pprof heap 分析分配热点。sync.Pool 复用高频临时对象。减少 []byte 和 string 的频繁创建。批量处理降低分配次数。' }], exercises: [{ level: '基础', task: 'GOGC 设成 200 有什么效果', hint: 'GC 频率', answer: 'GC 触发更少——允许堆内存增长更多。程序使用更多内存但 GC 暂停更少（适合内存不那么紧张的场景）。GOGC=off 完全关闭 GC——只在确信不会 OOM 时使用。' }] },
  "orpxzKHFcf1BDQSefHl6O": { mentalModel: 'sync.Pool 是"临时对象缓存"——把要重复使用的对象暂存，下次直接拿而不重新分配。减少 GC 压力。适合频繁创建/销毁的临时对象（如 buffer）。', sections: [{ title: 'sync.Pool', content: '创建：pool := sync.Pool{New: func() any { return new(bytes.Buffer) }}。获取：buf := pool.Get().(*bytes.Buffer)。放回：buf.Reset(); pool.Put(buf)。Get 拿到的对象是"上个 GC 周期剩下的"——GC 清空 Pool。适合：bytes.Buffer、序列化用的临时 struct、频繁使用的数据库 statement。不适合：有状态的对象、需要 init/reset 之外逻辑的。' }], diagnosis: [{ symptom: 'sync.Pool.Get() 返回的是上一个使用者修改过的对象', cause: 'Pool 中的对象可能是被 Put 回去的、残留上一轮数据的旧对象。', fix: 'Put 前必须 Reset 对象：buf.Reset(); pool.Put(buf)。Get 后即使 New 创建了也不依赖"干净"假设——始终在使用前初始化所需字段。' }, { symptom: 'sync.Pool 不适合存放有状态的连接（如 DB 连接）', cause: 'Pool 中的对象可能在任意时间被 GC 清除。连接需要生命周期管理和重连机制，Pool 不提供这些。', fix: '数据库连接用 sql.DB 内置连接池。网络连接用 conn pool 库（如 pgxpool）。sync.Pool 只适合无状态、可随时丢弃的临时对象。' }], exercises: [{ level: '基础', task: 'sync.Pool 里面的对象在 GC 后还存在吗', hint: 'Pool 会被 GC 清空', answer: '每次 GC 会清空 sync.Pool。GC 认为 Pool 中的对象是"可能不再需要的"——清空让 GC 回收内存。所以 Get 可能返回 nil（需要 New 函数创建）。这个设计防止 Pool 无限制增长。' }] },
  "qUykEUH1-9Dzotc_ltV3g": { mentalModel: 'Go 的 Select——不只是"选择 channel"，还有超时、非阻塞、无限等待等高级模式。', sections: [{ title: 'Select 模式', content: '超时：case <-time.After(5*time.Second):。非阻塞：default:。心跳：case <-ticker.C:。Done 模式：case <-ctx.Done(): return ctx.Err()。for-select 组合：在 for 循环内 select 持续监听多个 channel。random select：同时就绪→随机选择——天然负载均衡。' }], diagnosis: [{ symptom: 'select 中多个 case 同时就绪但期望的执行顺序没按预期', cause: 'select 在多个 case 同时就绪时随机选择——这是 Go 有意设计的公平机制。若依赖 case 顺序会导致非确定性行为。', fix: '不要依赖 select 的 case 顺序。如果必须按优先级，嵌套 select：select { case <-highPri: ...; default: select { case <-midPri: ...; default: select { case <-lowPri: ... } } }。' }, { symptom: 'select 的 default 分支导致 channel 操作丢失', cause: 'select { case ch <- msg: default: } 如果 ch 满，msg 被直接丢弃——无缓冲区、无错误提示。', fix: '需要可靠投递时不要用 default。用缓冲 channel + 日志告警：select { case ch <- msg: default: log.Warn("dropped msg") }。或用 ring buffer 实现背压。' }], exercises: [{ level: '基础', task: '用 select 实现"5 秒内收不到消息就超时"', hint: 'time.After', answer: 'select { case msg := <-ch: process(msg); case <-time.After(5 * time.Second): return errors.New("timeout") }' }] },
  "xUKsD2eTP9-RszHvKYp9Y": { mentalModel: 'Go 的 reflect 包——运行时类型反射。json.Marshal、gorm、依赖注入框架都在用 reflect。要小心——它绕过类型安全、慢、代码难懂。一般用代码生成替代。', sections: [{ title: 'reflect 的使用', content: 'reflect.TypeOf(x)：取类型。reflect.ValueOf(x)：取值（get/set）。tag 读取：field.Tag.Get("json")。方法调用：method.Call(args)。用途：序列化/反序列化、ORM、依赖注入、配置绑定。限制：不能动态改类型、私有没有反射的字段不可 set、性能开销。原则：能用代码生成（go generate）替代就不用手写 reflect。' }], diagnosis: [{ symptom: 'reflect.Value.Set 报"using unaddressable value" panic', cause: '直接对 reflect.ValueOf(x) 取的值不可寻址（CanAddr() 返回 false），无法修改。', fix: 'v := reflect.ValueOf(&x).Elem() 先取指针再 Elem() 获取可寻址值。或操作 struct 字段：v.FieldByName("Name").SetString("new")。' }, { symptom: 'reflect.DeepEqual 比较时间等包含非导出字段的 struct 结果不准', cause: 'reflect.DeepEqual 比较所有字段包括非导出字段。time.Time 包含非导出字段可能导致"看起来相等"的两个时间判定不等。', fix: '对 time.Time 用 t1.Equal(t2)。对自定义类型实现 Equal 方法。不要依赖 reflect.DeepEqual 处理复杂类型。' }], exercises: [{ level: '进阶', task: '为什么框架大量用 reflect 但业务代码该避免', hint: '类型安全+性能', answer: 'reflect 绕过编译时类型检查→运行时 panic 而非编译错误。reflect 操作比直接访问慢 10-100 倍。框架用它做泛型功能（json 序列化/orm 映射），业务代码该用具体类型和代码生成。' }] },
  "ywiNCAAvpSVXTwWwguxSZ": { mentalModel: 'Go 的 testing 包——不只是测试，还有 benchmark（性能）、example（示例文档）、fuzz（模糊测试）。go test 是测试、文档、性能、安全四合一工具。', sections: [{ title: 'Testing 进阶', content: 'Table-Driven Tests：Go 的标准测试模式——定义测试用例表→循环执行。t.Run(name, func(t *T))：子测试（并行+独立失败）。t.Parallel()：并行执行测试。TestMain：全局 setup/teardown。t.Helper()：标记辅助函数（错误报告指向上层）。' }], diagnosis: [{ symptom: 't.Run 中 parallel 子测试与循环变量闭包陷阱', cause: 'for _, tt := range tests { t.Run(tt.name, func(t *testing.T) { t.Parallel(); use(tt) }) } 中 tt 是循环变量，Go 1.21 之前 t.Parallel 闭包引用最终循环值。', fix: 'Go 1.22+ 自动修复。旧版本：tt := tt 在循环体开头做 shadow copy。或用 t.Run(tt.name, func(t *testing.T) { t.Parallel(); tt := tt }) }。' }, { symptom: 't.Fatal 在 t.Run 子测试中只终止当前子测试不终止父测试', cause: 't.Fatal/t.Fatalf 只终止调用它的那个 t（子测试或父测试）。子测试中的 Fatal 不影响其他子测试。', fix: '子测试的 Fatal 只停止该子测试——这是期望行为。需要父测试感知子测试失败：在 t.Run 外检查 t.Failed()。或用 assert/require 库。' }], exercises: [{ level: '进阶', task: '写一个 Table-Driven Test', hint: 'tests := []struct{...}; for _, tt := range tests { t.Run(...) }', answer: 'tests := []struct {name string; a, b, want int}{{"pos", 1, 2, 3}, {"neg", -1, 1, 0}, {"zero", 0, 0, 0}}; for _, tt := range tests { t.Run(tt.name, func(t *testing.T) { got := add(tt.a, tt.b); if got != tt.want { t.Errorf("add(%d,%d)=%d,want %d", tt.a, tt.b, got, tt.want) } }) }' }] },
};