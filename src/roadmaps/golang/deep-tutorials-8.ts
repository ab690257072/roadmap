import type { DeepTutorial } from '../deep-types';

export const GOLANG_DEEP_TUTORIALS_8: Record<string, DeepTutorial> = {
  "-M-qrXwVt7HfJv0CSRNGS": { mentalModel: 'iota 是 Go 的"自动编号器"——在 const 块中从 0 开始自增，每新增一行 const +1。用于定义枚举值（如 HTTP 状态码分组、权限级别）。比手动编号优雅且防错。', sections: [{ title: 'iota 用法', content: 'const ( A = iota; B; C ) → A=0, B=1, C=2。重置：每个 const 块内 iota 归零。跳过：const ( _ = iota; KB = 1 << (10 * iota); MB; GB ) → KB=1024, MB=1048576。复杂场景：const ( FlagRead = 1 << iota; FlagWrite; FlagExec ) → 1, 2, 4（位掩码）。' }], diagnosis: [{ symptom: '多个 const 块中 iota 值不从 0 开始', cause: '每个 const 块的 iota 独立从 0 开始计数。如果期望 iota 跨 const 块连续需要手动指定起始值。', fix: '同一枚举放同一个 const 块。需要在不同块中连续时：const ( X = iota + 10; Y; Z ) // 10, 11, 12。' }, { symptom: 'iota 配合表达式时中间跳过的值仍递增', cause: 'iota 每行 +1 与表达式无关。const ( _ = iota; A; B ) 中 _=0, A=1, B=2。即使 _ 被丢弃，A 也不是 0。', fix: '需要 A=0 时明确赋值：const ( A = iota; B; C )。需要跳过时 _ 能保留 iota 自增但丢弃值，这是设计意图。' }], exercises: [{ level: '基础', task: '用 iota 定义一周七天（Mon=1...Sun=7）', hint: 'iota + 1', answer: 'const ( Mon = iota + 1; Tue; Wed; Thu; Fri; Sat; Sun ) → Mon=1...Sun=7。iota+1 让第一个值从 1 而非 0 开始。' }] },
  "PRTou83_rD0u7p2elGG4s": { mentalModel: 'Go 的 bool 类型——只有 true 和 false（没有 truthy/falsy 的概念）。不能把 1/0 当 bool 用、不能 ! 非布尔值。这避免了 JavaScript 中 !!"false" == true 的混乱。', sections: [{ title: 'Bool 类型', content: 'var ok bool = true。if ok { ... }——条件表达式必须是 bool。Go 没有隐式类型转换——if 1 { ... } 编译错误！逻辑运算符：&& || !。bool 零值是 false。比较操作符返回 bool：== != < > <= >=。' }], diagnosis: [{ symptom: 'if x { ... } 中 x 不是 bool 类型导致编译错误', cause: 'Go 条件表达式必须是 bool 类型，if 不接受 int/string/pointer 等非 bool 类型。', fix: '显式比较：if x != 0 { ... } 或 if s != "" { ... }。指针：if p != nil { ... }。Go 不允许多值隐式转为 bool。' }, { symptom: '比较操作符不能链式使用（如 a < b < c）', cause: 'Go 的比较操作符返回 bool，不能连续比较。a < b < c 会被解析为 (a < b) < c，即 bool < int，类型不匹配。', fix: '分开写：if a < b && b < c { ... }。这和 Python（可以 a < b < c）不同。' }], exercises: [{ level: '基础', task: 'Go 和 JavaScript 在 bool 处理上最大的不同', hint: 'truthy', answer: 'Go 中条件表达式必须是准确的 bool 值。JS 中 if (1) 通过（1 是 truthy）。Go 中 if 1 编译错误——需要显式 if x != 0。这防止了无数隐式转换 bug。' }] },
  "Mn-M7kxOKEublJGitgjNA": { mentalModel: '复数（Complex Numbers）——Go 是少数原生支持复数的语言。complex64/128 用于科学计算和信号处理。大部分后端开发者永远用不到，但存在说明 Go 的"系统编程"血统。', sections: [{ title: 'Complex 类型', content: '声明：c := complex(3, 4) → 3+4i。简写：c := 3 + 4i。取实部/虚部：real(c), imag(c)。运算：+, -, *, / 直接支持。cmath 包：Abs/Phase/Polar/Conj 等高级操作。用途：DSP、FFT、物理模拟。后端开发极少用——了解存在即可。' }], diagnosis: [{ symptom: 'complex128 与 complex64 之间不能直接运算', cause: 'complex128 和 complex64 是不同的类型，Go 不允许隐式类型转换。', fix: '统一使用 complex128（推荐）。需要转换：c128 := complex128(c64)。数值字面量 3+4i 默认为 complex128。' }, { symptom: '复数之间不能用 == 比较（不等于 NaN 问题）', cause: 'complex64 和 complex128 是可比较类型，但 NaN 复数的比较行为令人困惑。real 或 imag 为 NaN 的复数不等于任何值（包括自身）。', fix: '比较前用 cmplx.IsNaN(c) 检查。浮点数比较用容差：cmplx.Abs(c1-c2) < epsilon。' }], exercises: [{ level: '基础', task: 'Go 支持复数说明了什么', hint: '语言设计目标', answer: 'Go 设计为"系统编程语言"——网络/操作系统/编译器/数据库。这些领域可能用到复数（信号处理/加密）。普通 Web 后端用不到但不影响——Go 里有你不会用的功能是正常的。' }] },
  "EzaH378F3kOdQbczhVjhA": { mentalModel: '浮点数（float32/float64）——牢记：浮点数不精确！0.1+0.2 ≠ 0.3 这坑所有语言都有。金额用整数（存分）或 decimal 库，绝对不用 float。', sections: [{ title: '浮点数的坑', content: 'fmt.Println(0.1+0.2 == 0.3) → false！原因：0.1 和 0.2 在二进制浮点中是无限循环小数。IEEE 754 精度限制。金额处理：存分为整数（29900 分 = 299.00 元），用 math/big.Rat 或 shopspring/decimal 库。NaN != NaN（无穷不等于自身——浮点规范）。' }], diagnosis: [{ symptom: 'float64 金额运算产生 0.000000001 的零头', cause: 'IEEE 754 浮点数无法精确表示十进制小数。0.1 在二进制中是无限循环小数。', fix: '金额用 int64 存分（29900 分 = 299.00 元）。需要精确小数用 math/big.Rat 或 shopspring/decimal。绝不用 float64 存金额。' }, { symptom: 'float64 比较结果为 false 但期望为 true', cause: '浮点数运算误差导致两个"应该相等"的值实际差一个极小量。3.0/7.0 * 7.0 可能不等于 3.0。', fix: '用容差比较：if math.Abs(a - b) < 1e-9。不要直接用 == 比较浮点数。' }], exercises: [{ level: '基础', task: '在 Go playground 中验证 0.1+0.2 是否等于 0.3', hint: 'fmt.Println(0.1+0.2 == 0.3)', answer: 'fmt.Println(0.1+0.2) → 0.30000000000000004。fmt.Println(0.1+0.2 == 0.3) → false。这是所有语言的浮点问题——IEEE 754 限制。' }] },
  "27lhNAdKwRrc2S2ha3QNG": { mentalModel: 'Go 测试覆盖率——go test -cover 告诉你测试覆盖了多少代码行。不是目标 100% 而是发现"哪些代码完全没有测试"。', sections: [{ title: 'Coverage 工具', content: 'go test -cover：总覆盖率百分比。go test -coverprofile=coverage.out：生成覆盖率数据文件。go tool cover -html=coverage.out：浏览器查看逐行覆盖情况（绿色=覆盖、红色=未覆盖）。目标：核心业务逻辑 > 80%，不追求 100%。' }], diagnosis: [{ symptom: '覆盖率 100% 但仍有 bug', cause: '覆盖率只说明代码被执行过，不说明测试覆盖了所有边界条件。100% 覆盖是必要非充分条件。', fix: '不代表不需要写更多测试。关注边界值、错误路径、并发场景。100% 覆盖只是起点不是终点。' }, { symptom: '覆盖报告显示的函数名与实际测试不对应', cause: 'go test 可能使用了缓存，旧的覆盖率数据未更新。', fix: 'go clean -testcache && go test -coverprofile=coverage.out ./...。或用 -count=1 禁用缓存。' }], exercises: [{ level: '基础', task: '生成 HTML 格式的覆盖率报告', hint: 'go test -coverprofile + go tool cover -html', answer: 'go test -coverprofile=coverage.out && go tool cover -html=coverage.out -o coverage.html && open coverage.html' }] },
  "ZSiNvYDTW2RYyPttls5iz": { mentalModel: 'init() 是 Go 的"自动执行函数"——每个包导入时 init() 自动运行（在 main() 之前）。用于初始化包级状态（注册驱动、加载配置）。不要乱用——隐式执行顺序难调试。', sections: [{ title: 'init() 规则', content: '每个包可以有多个 init()、每个文件可以有多个 init()。执行顺序：依赖包先 init→当前包 init→main()。同包内多个 init() 按文件名字母序+出现顺序执行。用途：注册 database/sql 驱动（import _ "github.com/lib/pq"）、初始化全局 logger、加载配置文件。滥用：业务逻辑放 init() → 测试困难、顺序依赖出错。' }], diagnosis: [{ symptom: '不同 init() 之间依赖执行顺序导致不确定行为', cause: 'init() 的执行顺序由文件名字母序决定，依赖文件名排序是很脆弱的——换个文件名就可能出错。', fix: '不要在多个 init() 之间建立依赖关系。如果需要按序初始化，用显式的 Initialize() 函数由 main 调。init() 只放独立的注册逻辑。' }, { symptom: 'init() 中有 panic 导致程序启动失败且无优雅错误处理', cause: 'init() 在 main() 之前执行，panic 会导致程序直接崩溃（甚至来不及 recover）。', fix: 'init() 中不使用 panic。用 log.Fatal 输出错误信息。或将初始化移到 main() 中做错误处理。Must* 模式（如 MustCompile）在 init 中可以接受。' }], exercises: [{ level: '基础', task: 'database/sql 的驱动注册为什么放在 init() 中', hint: 'import 副作用', answer: 'import _ "github.com/lib/pq" 触发 pq 包的 init() 调用 sql.Register("postgres", &Driver{})。这样 sql.Open("postgres", ...) 时能找到驱动。使用者只需要 import，不需要手动调用注册。' }] },
  "ai2s1bwiTcPzrXQTxFDwV": { mentalModel: 'Go 的 File I/O——os 包（底层文件操作）+ io/ioutil 包（读全部/写全部）+ bufio（缓冲区读写）。os.ReadFile/os.WriteFile 一步读/写文件——最简单常用。', sections: [{ title: '文件操作', content: '读整个文件：data, err := os.ReadFile("file.txt")——Go 1.16+。写整个文件：os.WriteFile("file.txt", data, 0644)。逐行读：bufio.NewScanner(file)。流式拷贝：io.Copy(dst, src)。defer file.Close() 确保文件关闭。错误处理每个文件操作——检查 err。' }], diagnosis: [{ symptom: 'os.ReadFile 读大文件导致 OOM', cause: 'os.ReadFile 一次性读取整个文件到内存。大文件（如 GB 级日志）会耗尽可用内存。', fix: '用 os.Open + io.Copy 或 bufio.NewScanner 流式处理。大文件逐行读：scanner := bufio.NewScanner(file); for scanner.Scan() { processLine(scanner.Text()) }。' }, { symptom: 'defer f.Close() 的错误被静默忽略', cause: 'defer f.Close() 返回值 error 未被检查。Close 可能因磁盘满、网络断开等原因失败（尤其是写操作），但 defer 无法传播 error。', fix: '写入文件时不用 defer Close，显式检查 Close 的 error。或 func() { if err := f.Close(); err != nil { ... } }。读写文件建议用 os.ReadFile/os.WriteFile 自动关闭。' }], exercises: [{ level: '基础', task: '用 os.ReadFile 读取文件并打印内容', hint: 'data, err := os.ReadFile(path)', answer: 'data, err := os.ReadFile("test.txt"); if err != nil { log.Fatal(err) }; fmt.Println(string(data))' }] },
  "XLj16RmCe4-nKHW52Ebhf": { mentalModel: 'go fmt 是 Go 的"格式化警察"——所有 Go 代码必须用 go fmt 格式化。不是建议——是工具。团队不需要争论代码风格（大括号换不换行）——go fmt 直接格式化，关闭所有讨论。', sections: [{ title: 'go fmt 规则', content: 'go fmt 自动：对齐缩进（tab 不是空格）、移除多余空格、空行整理。goimports：额外自动整理 import 语句（删除没用到的、添加缺失的）。gofmt -s：简化代码（如去掉不必要的括号）。IDE 保存时自动 go fmt——Git 仓库中的 Go 代码风格永远一致。' }], diagnosis: [{ symptom: 'go fmt 后 git diff 显示大量修改', cause: '同事的代码未用 go fmt 提交，或不同 IDE 配置了不同的格式化工具。', fix: '在 CI 中检查：gofmt -l . 并确保无输出（test -z "$(gofmt -l .)"）。Git pre-commit hook 自动 go fmt。团队统一配置 IDE 保存时 goimports。' }, { symptom: 'gofumpt 和 go fmt 输出不一致', cause: 'gofumpt 是 go fmt 的超集，规则更严格。不同工具之间的差异可能导致频繁的格式变更。', fix: '团队统一一个格式化工具。如果用 gofumpt，所有成员和 CI 都用 gofumpt。在 .golangci.yml 中配置使用 gofumpt linter。' }], exercises: [{ level: '基础', task: 'go fmt 为什么用 tab 而不是空格', hint: '明确性', answer: 'tab = 一个缩进层级（无法"部分缩进"）。空格可以放任意个数（意味不明确）。Go 团队明确选择 tab——不用讨论"两个空格还是四个空格"。' }] },
  "dC-1LotbW29C4zZJv6ak6": { mentalModel: 'Embedded Struct——Go 的"组合代替继承"机制。一个 struct 嵌入另一个 struct——外层的 struct 自动获得内层的方法和字段（像继承但本质是转发）。', sections: [{ title: 'Embedding 原理', content: 'type User struct { Name string }; type Admin struct { User; Level int }。admin.Name 直接访问（不需要 admin.User.Name）——但本质是转发。Admin 获得了 User 的所有方法和字段。同名冲突：外层覆盖内层。接口实现：如果 User 实现了 Stringer，Admin 也自动实现——因为方法被提升（promoted）。' }], diagnosis: [{ symptom: '嵌入多个类型有同名方法导致编译歧义', cause: 'type C struct { A; B } 且 A 和 B 都有 Who() 方法，编译器无法确定 c.Who() 应调用哪个，报"ambiguous selector"。', fix: '显式指定：c.A.Who() 或 c.B.Who()。或在 C 上定义 Who() 方法覆盖嵌入方法。避免嵌入有同名方法的类型。' }, { symptom: '嵌入指针类型未初始化导致 nil pointer dereference', cause: 'type Dog struct { *Animal } 中如果未初始化 Animal，调用 d.Speak()（提升的方法）会因 *Animal 是 nil 而 panic。', fix: '初始化嵌入指针：d := Dog{Animal: &Animal{Name: "Buddy"}}。在方法中检查 nil：if d.Animal == nil { return "" }。或用值嵌入替代：type Dog struct { Animal }。' }], exercises: [{ level: '基础', task: 'Embedding 和继承有什么区别', hint: '组合', answer: '继承："Admin 是一个 User"——子类和父类是 is-a 关系，紧密耦合。Embedding："Admin 包含一个 User"——has-a 关系，松耦合，只是语法上做了方法的自动转发。Go 明确选了组合而非继承。' }] },
};