import type { DeepTutorial } from '../deep-types';

export const DEVOPS_DEEP_TUTORIALS_2: Record<string, DeepTutorial> = {
  "0_GMTcMeZv3A8dYkHRoW7": { mentalModel: 'Apache HTTP Server 是 Web 服务器"鼻祖"——1995 年诞生，辉煌时 70% 网站用 Apache。现在退居 Nginx 之后，但在共享主机和 .htaccess 场景仍然活跃。', sections: [{ title: 'Apache 在 DevOps 中的角色', content: 'Apache 在传统运维中很常见：cPanel/Plesk 默认 Apache、PHP（mod_php）直接集成 Apache。.htaccess 文件让每个目录有自己的配置规则（适合共享主机多用户自管理）。对 DevOps：了解 Apache 能看懂老系统配置，新项目用 Nginx/Caddy。' }], diagnosis: [{ symptom: 'Apache .htaccess 导致每次请求递归查找文件导致性能下降', cause: 'Apache 对每个目录检查 .htaccess，深层目录嵌套时一次请求多次文件系统查找', fix: '禁用 .htaccess（AllowOverride None），规则放在主配置 Directory 块中，一次加载到内存' }, { symptom: 'Apache KeepAlive 耗尽 worker 线程', cause: 'KeepAliveTimeout 过大时，空闲连接占用 worker 导致新请求无法处理', fix: 'KeepAliveTimeout 设 2-5s。MaxKeepAliveRequests 限制每连接请求数。高并发场景换 Nginx' }], exercises: [{ level: '基础', task: 'Apache 的 .htaccess 和 Nginx 的配置有什么区别', hint: '分布式 vs 集中', answer: '.htaccess 是分布式——每个目录下可以有独立配置文件。Nginx 配置是集中的——在 server/location 块中统一管理。.htaccess 灵活但慢（每次请求都要递归查找和解析），Nginx 集中配置快但灵活性差。' }] },
  "54UZNO2q8M5FiA_XbcU_D": { mentalModel: 'Caddy 是"自带 HTTPS"的新一代 Web 服务器——用 Go 写、零配置自动 TLS 证书。配置文件从 Nginx 的几十行变成 4 行。DevOps 的"省心之选"。', sections: [{ title: 'Caddy 在 DevOps 中的应用', content: 'Caddyfile 示例：example.com { reverse_proxy localhost:3000 }——就三行！自动申请 Let\'s Encrypt 证书、自动续签、自动 HTTPS 重定向。Caddy API：/config/ 端点动态改配置而不需要 reload（Nginx 需要 nginx -s reload）。适合：小型项目、需要快速部署、不想管证书到期问题。Nginx 更适合：K8s Ingress、大规模生产、复杂路由规则。' }], diagnosis: [{ symptom: 'Caddy ACME 挑战失败导致证书无法自动签发', cause: 'Let\'s Encrypt HTTP-01 挑战要求 80 端口可达——防火墙或云 LB 屏蔽了 80 端口', fix: '开放 80 端口（仅用于证书挑战）。或改用 DNS-01 挑战（API 操作 DNS 记录）' }, { symptom: 'Caddyfile 中 reverse_proxy 目标不健康导致 502 错误', cause: '后端服务挂掉或未启动——Caddy 没有主动健康检查', fix: 'Caddy 支持健康检查：reverse_proxy localhost:3000 { health_uri /health health_interval 10s }。搭配 systemd 自动重启后端' }], exercises: [{ level: '基础', task: '用 Caddy 为什么不需要 certbot', hint: 'Caddy 内置什么能力', answer: 'Caddy 内置 Let\'s Encrypt（ACME）客户端——检测到配置中的域名→自动向 Let\'s Encrypt 申请证书→自动续签（过期前 30 天）。完全不需要手动 certbot 或 cron 续签脚本。' }] },
  "5iJOE1QxMvf8BQ_8ssiI8": { mentalModel: 'IIS（Internet Information Services）是微软的"Windows 专属 Web 服务器"——在 .NET/Windows 生态中相当于 Nginx+Node.js 的组合。DevOps 在混合环境（Linux+Windows）中迟早会遇到。', sections: [{ title: 'IIS 在 DevOps 中的定位', content: 'IIS 是 Windows Server 的内置组件。管理方式：IIS Manager（GUI）、PowerShell（命令行）、appcmd（CLI 工具）。和 K8s 的关系：Windows 容器可以在 K8s 上跑 .NET Framework + IIS（但较重）。现代 .NET Core/.NET 6+ 已跨平台——用 Kestrel（自带的 Web 服务器）+ Nginx 反向代理，不再依赖 IIS。IIS 主要是老系统的维护场景。' }], diagnosis: [{ symptom: 'IIS 应用池(App Pool)崩溃导致站点 503 无法访问', cause: '内存泄漏或异常导致 w3wp.exe 崩溃，应用池默认不自动快速恢复', fix: '配置快速故障保护：5 次故障/5 分钟触发自动重启。设置定时回收（凌晨低峰期每天回收）' }, { symptom: 'IIS 和 Kestrel 混用导致端口冲突', cause: 'Windows 上 IIS 和 .NET Kestrel 同时监听相同端口', fix: 'IIS 做反向代理到 Kestrel 监听 127.0.0.1:5000，IIS 监听 0.0.0.0:80/443 转发到 localhost:5000' }], exercises: [{ level: '基础', task: '现代 .NET 应用还需要 IIS 吗', hint: 'Kestrel', answer: '.NET Core 起就不需要 IIS 了。Kestrel 是跨平台的内置 Web 服务器，前面放 Nginx/Caddy 做反向代理即可。IIS 只在必须用 .NET Framework（Windows 独占）的老项目中需要。' }] },
  "AJO3jtHvIICj8YKaSXl0U": { mentalModel: 'HTTPS 是"加密传输的默认标配"——不再是可选项。Let\'s Encrypt 免费证书+自动化续签让 HTTPS 零成本、零理由不开启。DevOps 的部署清单上这是第一行。', sections: [{ title: 'HTTPS 配置实践', content: 'Nginx：listen 443 ssl; ssl_certificate ...; ssl_certificate_key ...;。Caddy：零配置自动 HTTPS。Certbot：Let\'s Encrypt 命令行工具——certbot --nginx 自动获取并配置 Nginx。安全强化：只启用 TLS 1.2+（ssl_protocols TLSv1.2 TLSv1.3;）、HSTS（add_header Strict-Transport-Security "max-age=63072000"）。Mozilla SSL Configuration Generator 自动生成安全配置。' }], diagnosis: [{ symptom: 'HTTPS 证书即将过期但无人察觉导致全站安全警告', cause: 'Let\'s Encrypt 90 天有效期且 certbot renew 定时任务静默失败', fix: 'certbot renew --dry-run 每周测试。监控即将过期（30d/7d/1d 分级告警）。Caddy 内置自动续签更省心' }, { symptom: 'HTTPS 启用后混合内容(Mixed Content)导致浏览器锁图标消失', cause: 'HTTPS 页面加载了 HTTP 资源（img/script）导致浏览器阻止或警告', fix: 'Content-Security-Policy: upgrade-insecure-requests 自动升级。检查代码中 HTTP 硬编码改为 HTTPS 或协议相对 URL' }], exercises: [{ level: '基础', task: '用 certbot 为一个 Nginx 站点配置 HTTPS', hint: 'certbot --nginx -d example.com', answer: 'sudo apt install certbot python3-certbot-nginx && sudo certbot --nginx -d example.com -d www.example.com。certbot 自动修改 Nginx 配置+获取证书+设置自动续签定时任务。' }] },
  "CQhUflAcv1lhBnmDY0gaz": { mentalModel: '容器是 DevOps 的"标准化打包箱"——应用+所有依赖→一个镜像。开发/测试/生产同一个箱子——"在我机器上能跑"从此消失。Docker 是容器的"标准答案"。', sections: [{ title: '容器在 DevOps 流水线中', content: 'Build：Dockerfile→docker build→Image。Ship：docker push→Registry（Docker Hub/ECR/ACR/Harbor）。Run：docker run/docker compose up/K8s deploy。容器化 = CI/CD 的基础——Git Push→CI 构建镜像→推 Registry→CD 拉镜像部署。环境一致性：dev 容器=staging 容器=prod 容器（同一个镜像），只有配置（环境变量）不同。' }], diagnosis: [{ symptom: 'Docker 构建时 COPY . . 导致镜像包含 node_modules、.git、密钥', cause: '构建上下文不加以限制导致整个项目目录都打进镜像', fix: '写入 .dockerignore：node_modules/ .git/ .env* *.log。多阶段构建分离构建与运行环境' }, { symptom: '容器内应用监听 127.0.0.1:3000 导致宿主机无法访问', cause: '容器内 localhost 是容器自己的网络命名空间导致外部无法路由', fix: '应用监听 0.0.0.0:3000。Dockerfile 中 EXPOSE 3000。docker run -p 8080:3000 做端口映射' }], exercises: [{ level: '基础', task: '为一个 Node.js 应用写一个生产级的 Dockerfile', hint: '多阶段构建', answer: 'FROM node:20-alpine AS builder\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\n\nFROM node:20-alpine\nWORKDIR /app\nCOPY --from=builder /app/dist ./dist\nCOPY --from=builder /app/node_modules ./node_modules\nUSER node\nEXPOSE 3000\nCMD ["node","dist/server.js"]' }] },
  "FE2h-uQy6qli3rKERci1j": { mentalModel: 'AWS ECS / Fargate 是 AWS 的容器服务——ECS 管容器编排，Fargate 管底层服务器（Serverless 容器）。不需要自己管理 K8s 集群和 EC2 节点。', sections: [{ title: 'ECS vs K8s', content: 'ECS：AWS 原生容器编排，和 AWS 服务深度集成（ALB/CloudWatch/IAM），学习曲线低。Fargate：不需要管理底层 EC2——"我要跑 3 个容器，每个 2 vCPU 4GB 内存"，AWS 自动分配计算资源，按使用量付费。K8s（EKS）：功能最强但最复杂——适合多环境、需要精细控制的场景。选择：简单应用→ECS Fargate、已有 K8s 知识/需要多云→EKS。' }], diagnosis: [{ symptom: 'Fargate 任务一直处于 PENDING 状态无法启动', cause: '没有足够的 ENI（弹性网络接口）可用——子网 IP 已耗尽', fix: '检查子网可用 IP 数量。Fargate 每个任务占用 1 个 ENI。扩容子网 CIDR 或使用多 AZ 部署' }, { symptom: 'ECS Task 启动后立即退出导致反复 PENDING 到 STOPPED 循环', cause: '容器启动立即退出——可能是入口命令错误、依赖不可用或健康检查失败', fix: 'CloudWatch Logs 查看退出原因。本地 docker run 同镜像验证。检查 secrets/环境变量是否正确配置' }], exercises: [{ level: '基础', task: 'ECS Fargate 相比 EKS 最大的优势', hint: '运维', answer: 'Fargate 不需要管理服务器/K8s 集群——不用打补丁、不用升级 K8s 版本、不用管理节点组。你只管容器定义（Task Definition）→ 告诉 AWS 跑几个 → AWS 搞定底层。运维负担从"管一个 K8s 集群"降到"管一个容器配置"。' }] },
  "P0aM9tD6fYxU2iL5sB0cV": { mentalModel: '容器镜像管理——Registry 是放镜像的"仓库"。Docker Hub（公共）、AWS ECR（AWS 内网快）、Harbor（企业自建——安全合规）。', sections: [{ title: 'Image Registry 选型', content: 'Docker Hub：公共镜像库、免费用户有限额。ECR/GCR/ACR：云厂商的私有 Registry——和云环境高速互联、IAM 权限控制。Harbor：企业自建 Registry——镜像扫描（漏洞检测）、RBAC、复制策略。GitHub Container Registry（ghcr）：和 GitHub Actions CI/CD 深度集成。镜像策略：Immutable Tags（同 tag 不可覆盖——防意外篡改）、漏洞扫描（CI 中集成 Trivy/Snyk）。' }], diagnosis: [{ symptom: 'docker push 极慢——Registry 在国外跨洋传输', cause: 'Docker Hub/AWS ECR 在美国导致国内拉取推送从几十 MB/s 变成几百 KB/s', fix: '使用镜像加速（阿里云/DaoCloud）。ECR 在同 region VPC 内免流量费且高速。企业内网自建 Harbor' }], exercises: [{ level: '基础', task: '推送 Docker 镜像到 ECR 的完整流程', hint: 'aws ecr get-login + docker tag + docker push', answer: 'aws ecr get-login-password | docker login --username AWS --password-stdin $ACCOUNT.dkr.ecr.$REGION.amazonaws.com && docker tag myapp:latest $ACCOUNT.dkr.ecr.$REGION.amazonaws.com/myapp:latest && docker push $ACCOUNT.dkr.ecr.$REGION.amazonaws.com/myapp:latest' }] },
  "RDLmML_HS2c8J4D_U_KYe": { mentalModel: 'CI/CD 流水线设计——不是"装个 Jenkins 就完事"。好的 CI/CD Pipeline 是分阶段（Build→Test→Security Scan→Deploy→Verify）的自动化流水线。', sections: [{ title: 'CI/CD 最佳实践', content: '典型 Pipeline 阶段：1) Build：编译/安装依赖 2) Lint + Test：语法检查+单元测试+集成测试 3) 安全扫描：依赖漏洞(Trivy/Snyk)、SAST（SonarQube）4) 构建容器镜像 5) 推送到 Registry 6) 部署到 Staging 7) Smoke Test（部署后验证）8) 部署到 Production（可选金丝雀/蓝绿策略）。Pipeline 时间：目标 < 10 分钟（否则开发者 push 后等太久——开始跳过 CI 直接 merge）。' }], diagnosis: [{ symptom: 'CI Pipeline 运行 30 分钟导致开发者绕过 CI 直接 merge', cause: '每次 push 等太久导致开发者不再关注 CI 结果，CI 失去门禁作用', fix: '优化 Pipeline：并行 job、缓存 node_modules/.m2/、增量编译。目标小于 10 分钟。测试分层：快速单元测试先跑' }, { symptom: 'CD 部署到生产失败后无法快速回滚', cause: '部署策略仅是 kubectl apply 没有保留旧版本', fix: '金丝雀/蓝绿部署。K8s Deployment revisionHistoryLimit 保留多个旧 ReplicaSet。kubectl rollout undo 一键回滚' }], exercises: [{ level: '基础', task: '为什么 CI Pipeline 时间应该尽量短', hint: '开发者行为', answer: 'Pipeline 时间长→开发者 push 后等 30 分钟才知道结果→等不起→跳过 CI 直接 merge→CI 流过形式→出问题没人看。Pipeline < 10 分钟 → push 后去接杯咖啡回来就有结果→养成了看 CI 结果的习惯→CI 真正起到门禁作用。' }] },
  "w5d24Sf8GDkLDLGUPxzS9": { mentalModel: '环境管理——Dev/Staging/Prod 三个环境的隔离与同步。"Staging 和 Prod 一模一样"是最终目标，但现实总有差距。', sections: [{ title: '环境管理策略', content: 'Dev：开发测试用、数据可以随意破坏、快速迭代。Staging：和生产一致配置、做上线前最后验证。Prod：正式环境、所有变更经过 Staging 验证。环境漂移（Drift）：手动改动导致环境不一致。解决：Infrastructure as Code（Terraform/Pulumi）——环境配置版本化管理、GitOps（ArgoCD/Flux）——Git 是唯一真实状态来源。' }], diagnosis: [{ symptom: '环境漂移——Staging 测试通过但生产部署失败', cause: '运维手动 SSH 改过生产配置导致几个月后 Staging 和生产完全不同', fix: '禁止手动 SSH 操作。所有变更通过 Terraform/Ansible CI Pipeline 执行。Git 是唯一真实状态来源（GitOps）' }, { symptom: '多环境变量管理混乱导致 .env 文件满天飞', cause: 'dev.env/staging.env/prod.env 四处散落，总有遗漏或写错', fix: '集中管理：HashiCorp Vault/AWS Secrets Manager/K8s ConfigMap+Secret。环境间差异最小化' }], exercises: [{ level: '基础', task: '什么是环境漂移', hint: '手动改服务器', answer: '环境漂移=手动 SSH 到服务器改配置→今天改的、明天同事又改了别的→几周后 dev 和 prod 已经不一样。部署到 prod 出问题是因为"dev 上能跑但 prod 环境不一样"。防止：禁止手动 SSH 改配置、所有变更通过 Terraform/CI 执行。' }] },
  "qYRJYIZsmf-inMqKECRkI": { mentalModel: '监控和告警——不是"装个 Prometheus"就完事。好的监控是：知道什么指标重要、知道阈值设多少、知道告警了该怎么办。', sections: [{ title: '监控告警最佳实践', content: 'Golden Signals：延迟/流量/错误/饱和度。告警规则：1) 基于症状而非原因（告警"用户登录失败率>1%"而不是"CPU>80%"）2) 有 actionable（告警了知道该干嘛）3) 消除噪音（频繁震荡的告警 → 调阈值/加聚合/静默期）。告警升级：Warning→Critical→Page oncall。告警疲劳是 DevOps 的最大敌人——告警太多 → oncall 忽略 → 真正的故障漏掉。' }], diagnosis: [{ symptom: '告警疲劳导致 oncall 开始无视告警', cause: '告警阈值太低导致每天几百条——真正的故障混在噪音中漏掉', fix: '基于症状（非原因）告警。使用 Golden Signals。告警聚合+静默期。告警升级链：Warning→Critical→Page' }, { symptom: 'Prometheus 内存暴涨——Grafana 面板查询了大范围时间窗口', cause: 'Grafana 面板查询 rate(http_requests_total[30d]) 加载 30 天原始数据', fix: '合理设置默认范围：dashboard 默认 1h/6h。用 recording rules 预聚合大时间窗口。限制每个查询最大样本数' }], exercises: [{ level: '基础', task: '为什么告警应该"基于症状而非原因"', hint: '用户关心什么', answer: '"用户登录失败率高"是症状（用户直接受影响）。"数据库连接数高"是原因（没有直接影响用户，可能只是流量大了）。基于症状的告警 = 只告有业务影响的问题。基于原因的告警 = 在正常波动时也告警→告警疲劳。' }] },
  "oQIB0KE0BibjIYmxrpPZS": { mentalModel: 'SSL/TLS 证书管理——证书不是"申请一次用三年"就忘了，而是需要监控到期时间、自动化续签、统一管理的持续运维流程。', sections: [{ title: '证书管理', content: 'Let\'s Encrypt：90 天有效期→自动续签→最好用的免费方案。Cert Manager（K8s）：自动为 Ingress 申请和管理 Let\'s Encrypt 证书。证书到期监控：Prometheus Blackbox Exporter 检查证书剩余天数。私有 CA：企业内部用（如 HashiCorp Vault PKI）——签发内部服务间 mTLS 证书。证书吊销（CRL/OCSP）——证书泄露后立刻吊销。' }], diagnosis: [{ symptom: 'SSL 证书到期导致全站不可用——Let\'s Encrypt renew 失败', cause: 'certbot renew cron job 静默失败导致 90 天后证书过期无人知晓', fix: 'UptimeRobot/Prometheus Blackbox Exporter 监控证书到期。到期前 30d/7d/1d 分级告警。Caddy 内置自动续签更省心' }, { symptom: '中间证书未安装导致移动端/部分浏览器报证书错误', cause: '只安装了服务器证书而缺少中间 CA 证书链', fix: '用 SSL Labs 测试验证证书链完整性。Nginx 使用 fullchain.pem（包含中间证书）而非 cert.pem' }], exercises: [{ level: '基础', task: '用 openssl 检查一个域名的证书剩余天数', hint: 'openssl s_client + openssl x509 -enddate', answer: 'echo | openssl s_client -servername example.com -connect example.com:443 2>/dev/null | openssl x509 -noout -enddate' }] },
  "i8Sd9maB_BeFurULrHXNq": { mentalModel: '蓝绿部署——同时运行两套环境（蓝=当前 prod、绿=新版本），验证绿色无误后流量一键切过去。如果出问题，一键切回蓝色。零停机、即时回滚。', sections: [{ title: '蓝绿部署实践', content: '实现方式：两个独立的 K8s Deployment（blue 标签 vs green 标签），Service 的 selector 指向其中一个。切换 = 改 Service selector 从 blue → green。DNS/负载均衡器层面也能实现。优势：即时回滚（切换 selector 回来即可）、部署风险低。代价：需要两倍资源（两套环境同时运行）。适合：关键服务、不可接受停机的系统。' }], diagnosis: [{ symptom: '蓝绿部署切换时静态资源 404 找不到', cause: '新旧版本引用不同版本的 JS/CSS 文件名导致 CDN 中只有新版本或旧版本', fix: '静态资源（JS/CSS）先于应用发布。或保留旧版本资源一定时间。使用不可变文件名配合长缓存策略' }, { symptom: '蓝绿部署后数据库 schema 不兼容导致旧版本应用报错', cause: '切换流量到绿色但绿色改了 DB schema——旧版本还在运行且不理解新字段', fix: 'Schema 变更保持向后兼容：只能加列（不允许删/改列）。部署顺序：先加列→蓝绿切换→稳定后清理旧列' }], exercises: [{ level: '基础', task: '蓝绿部署和滚动更新有什么区别', hint: '切换 vs 逐个', answer: '滚动更新：逐个替换 Pod（新老同时存在、渐进式切换）。蓝绿部署：两套完整环境，一次性切换流量。蓝绿回滚更快（秒级），但资源消耗更大。滚动更新平滑但回滚慢（逐个回滚到旧版）。' }] },
  "qYRJYIZsmf-inMqKECRkI": { mentalModel: '金丝雀部署——先让 5% 的流量走新版本，观察 15 分钟（错误率/延迟正常），逐步增加→50%→100%。用真实用户的少量流量验证新版本，出问题仅影响 5% 用户。', sections: [{ title: '金丝雀 vs 蓝绿', content: '蓝绿：全量切换→全部用户看到新旧中的一个版本。金丝雀：渐进式→少部分用户先看到新版→逐步扩大。K8s 实现：Istio/Flagger 控制流量分配（VirtualService 的 weight 字段）。监控自动判断：金丝雀版本错误率>阈值→自动回滚（Flagger 的 Canary CRD）。金丝雀更适合"不确定新版本稳不稳"的场景。' }], diagnosis: [{ symptom: '金丝雀 5% 部署错误率正常但 100% 后故障爆炸', cause: '金丝雀 5% 流量太小未触发缓存击穿/数据库瓶颈——全量后暴雷', fix: '金丝雀阶段逐步增大流量（5%→25%→50%→100%），每步观察 10-15 分钟。同时监控资源使用趋势而非仅错误率' }, { symptom: '金丝雀分析时新版本响应时间偏慢实际是启动预热', cause: '新 Pod 刚启动 JIT 编译/连接池初始化导致前几分钟性能差异', fix: '预热机制：启动后先压测预热 30s-2min，或 replicas 分批滚动等预热完成后再进入金丝雀阶段' }], exercises: [{ level: '进阶', task: '用 Istio VirtualService 实现 90/10 金丝雀流量分配', hint: 'weight: 90 vs weight: 10', answer: 'apiVersion: networking.istio.io/v1beta1\nkind: VirtualService\nspec:\n  http:\n  - route:\n    - destination:\n        host: myservice\n        subset: v1\n      weight: 90\n    - destination:\n        host: myservice\n        subset: v2\n      weight: 10' }] },
};